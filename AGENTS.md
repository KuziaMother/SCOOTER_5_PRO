# AGENTS.md — правила работы в проекте SCOOTER_5_PRO

## Правило непрерывности (главное)
**После commit + push (или после compact/сжатия контекста) — НЕ останавливаться:
немедленно продолжать работу по текущему направлению.**

Текущее направление (по состоянию на 2026-08-18): **разбор MCU `research_bin/mcu_0007.bin`**.
Очередь задач — в конце этого файла. После compact: перечитать этот раздел + последние
разделы `research_bin/REPORT.md`, затем продолжить с первой незакрытой задачи.

## Как работать
- Все находки фиксировать в `research_bin/REPORT.md` (новые §, обновлять TOC).
- Инструменты — в `research_bin/*.py` (капстан/юникорн; эмулятор-харнес:
  `emulator/mcu_emu.py`, класс `McuEmu`; кодовые секции и список функций:
  `gen_functions_mcu.py` / `functions_mcu/`).
- Конвейер: разобрать → записать в REPORT.md → `git add` (скрипты + REPORT.md; .bin и
  __pycache__ НЕ коммитить) → `git commit -m "MCU §NN: …"` → `git push` → продолжить.
- Язык отчётов — русский. Консоль Windows cp1251 — не пугаться кривых кириллиц в выводе.
- RAM-база MCU = 0x20000000; flash-зеркало кода исполняется с баз 0x0 и 0x08000000.

## Очередь задач MCU (что осталось)
~~1. Семья кадров 0x5a — разобрана, см. REPORT §24.~~
2. **Источники ADC**: кто пишет raw i16 @0x278/@0x284/@0x29c и i16@0x200017a0
   (исходное значение батареи, маппинг [415..535]→[0..100]% — §22.3); ADC-машина 0x1a31c.
   Связь: i16@0x272 (окно [1876..2275], §24) и мастер u16@0x1e8 (0x5a-сессия, §24).
3. **u16@0x32e/0x330** (get 0x36): писатель не найден (§23.4).
4. **Таблицы @0x200016aa** (построение по subcmd 0x1a) — смысл интерполяции.
5. **Планировщик TX** @0x20000a43: как дескрипторы уходят в USART3 (0x1f600/0x1f6b4).
6. Заголовки 0x52/0x53/0x54 — проверить отдельно от 0x51 (общий диспетчер, но payload
   может различаться).
7. После MCU: прошивка BLE-чипа `research_bin/ble_2.7.0_0015.bin` (генерирует payload'ы
   кадров 0x42..0x46).

## Ключевые факты (шпаргалка)
- Main-loop связи: `0x1dfd8` (~1 кГц): статус → RTC(0x2186c) → battery-% → RX(0x1e9e0) →
  TX-сборка(0x1f1cc) → агрегатор(0x1f71c) → push(0x211f8).
- Протокол USART3: RX `74 CMD LEN DATA CHK 8B` (класс A, телеметрия 0x41..0x4A/0x60/0x61);
  данные-канал `51-54/5a/64` (RX-кольцо @0xc05, 3×150Б); TX MCU→BLE: запросы
  `63 CMD CHK 9C`, push `61 30/31 … CHK 9E`, ответы приложения `LEN 64/65 SUB DLEN DATA
  CHK 0xFF-HDR` (TX-кольцо @0x10b5).
- RTC: @0x2b0..0x2ba (год u16BE/месяц-ниббл/день/час/мин/сек, високосный); задаётся RX 0x41
  и данные-кадром 0x51/0x14.
- Батарея: SoC % @0x306 (из i16@0x200017a0, [415..535]→[0..100]); температура °C @0x30c
  (холод −5..1, перегрев ≥46); запас хода в 0x1d898.
- Версия MCU = "0007" (subcmd 0x21); CRC32-самопроверка — subcmd 0x2b.
