# func_0x099d4

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x0800099d4) | `0x000099d4` |
| размер кода | 6 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40003000 — периферия (r1)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- `func_0x09a20` (bl @0x00009a34)


## Дизассембляция

```asm
  099d4:  ldr r1, [pc, #4]                  -> периферия
  099d6:  str r0, [r1, #8]                  
  099d8:  bx lr                             
  ; --- literal-пул @0x099dc (1 слов) — ВНЕ границ функции ---
  099dc:  .word 0x40003000  ; периферия
```
