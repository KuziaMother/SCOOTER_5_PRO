# func_0x0c4b4

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x08000c4b4) | `0x0000c4b4` |
| размер кода | 18 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40021000 — периферия (r2)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  0c4b4:  mov r1, r0                        
  0c4b6:  movs r0, #0                       
  0c4b8:  ldr r2, [pc, #0xc]                -> периферия
  0c4ba:  ldr r0, [r2, #4]                  
  0c4bc:  bic r0, r0, #0xf0                 
  0c4c0:  orrs r0, r1                       
  0c4c2:  str r0, [r2, #4]                  
  0c4c4:  bx lr                             
  ; --- literal-пул @0x0c4c8 (1 слов) — ВНЕ границ функции ---
  0c4c8:  .word 0x40021000  ; периферия
```
