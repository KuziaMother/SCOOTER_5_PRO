# func_0x0c598

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x08000c598) | `0x0000c598` |
| размер кода | 20 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40021000 — периферия (r2)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  0c598:  mov r1, r0                        
  0c59a:  movs r0, #0                       
  0c59c:  ldr r2, [pc, #0xc]                -> периферия
  0c59e:  ldr r0, [r2, #4]                  
  0c5a0:  bic r0, r0, #0x3800               
  0c5a4:  orr.w r0, r0, r1, lsl #3          
  0c5a8:  str r0, [r2, #4]                  
  0c5aa:  bx lr                             
  ; --- literal-пул @0x0c5ac (1 слов) — ВНЕ границ функции ---
  0c5ac:  .word 0x40021000  ; периферия
```
