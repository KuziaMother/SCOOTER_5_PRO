# func_0x099e0

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x0800099e0) | `0x000099e0` |
| размер кода | 10 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40003000 — периферия (r1)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- `func_0x09a20` (bl @0x00009a3c)


## Дизассембляция

```asm
  099e0:  movw r0, #0xcccc                  
  099e4:  ldr r1, [pc, #4]                  -> периферия
  099e6:  str r0, [r1]                      
  099e8:  bx lr                             
  ; --- literal-пул @0x099ec (1 слов) — ВНЕ границ функции ---
  099ec:  .word 0x40003000  ; периферия
```
