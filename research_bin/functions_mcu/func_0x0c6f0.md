# func_0x0c6f0

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x08000c6f0) | `0x0000c6f0` |
| размер кода | 6 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x42420000 — периферия (r1)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  0c6f0:  ldr r1, [pc, #4]                  -> периферия
  0c6f2:  str r0, [r1, #0x60]               
  0c6f4:  bx lr                             
  ; --- literal-пул @0x0c6f8 (1 слов) — ВНЕ границ функции ---
  0c6f8:  .word 0x42420000  ; периферия
```
