# func_0x03970

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x080003970) | `0x00003970` |
| размер кода | 28 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x20000098 — RAM (r0)

## Вызовы (callees)

- `func_0x08a90` (0x00008a90, bl)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  03970:  push {r2, r3, r4, lr}             
  03972:  mov r0, sp                        
  03974:  bl #0x8a90                        -> func_0x08a90
  03978:  ldr r0, [pc, #0x10]               -> RAM
  0397a:  ldr r1, [sp]                      
  0397c:  str r1, [r0]                      
  0397e:  ldrh.w r1, [sp, #4]               
  03982:  strh r1, [r0, #4]                 
  03984:  ldrb.w r1, [sp, #6]               
  03988:  strb r1, [r0, #6]                 
  0398a:  pop {r2, r3, r4, pc}              
  ; --- literal-пул @0x0398c (1 слов) — ВНЕ границ функции ---
  0398c:  .word 0x20000098  ; RAM
```
