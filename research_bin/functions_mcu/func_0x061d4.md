# func_0x061d4

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x0800061d4) | `0x000061d4` |
| размер кода | 12 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40022000 — периферия (r1)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- `func_0x06230` (bl @0x00006238)


## Дизассембляция

```asm
  061d4:  ldr r1, [pc, #8]                  -> периферия
  061d6:  ldr r1, [r1, #0xc]                
  061d8:  orrs r1, r0                       
  061da:  ldr r2, [pc, #4]                  -> периферия
  061dc:  str r1, [r2, #0xc]                
  061de:  bx lr                             
  ; --- literal-пул @0x061e0 (1 слов) — ВНЕ границ функции ---
  061e0:  .word 0x40022000  ; периферия
```
