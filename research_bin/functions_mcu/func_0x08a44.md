# func_0x08a44

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x080008a44) | `0x00008a44` |
| размер кода | 8 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x20000f64 — RAM (r0)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- `func_0x1337c` (bl @0x000133d4)


## Дизассембляция

```asm
  08a44:  ldr r0, [pc, #4]                  -> RAM
  08a46:  ldr.w r0, [r0, #6]                
  08a4a:  bx lr                             
  ; --- literal-пул @0x08a4c (1 слов) — ВНЕ границ функции ---
  08a4c:  .word 0x20000f64  ; RAM
```
