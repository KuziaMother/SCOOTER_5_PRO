# func_0x004e0

| | |
|---|---|
| offset в файле | `0x004e0` |
| vaddr (база 0x01800000) | `0x018004e0` |
 | размер кода | 32 Б |
| регион | заголовок + bootloader (PLAIN) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x00202044 — RAM (r0)

## Вызовы (callees)

- 0x01802a1e (bl, вне списка функций)

## Кто вызывает (callers / xrefs)

- (не найден в открытых регионах)

## Дизассембляция

```asm
  018004e0:  push {r4, lr}                     
  018004e2:  mov r4, r2                        
  018004e4:  ldr r0, [pc, #0x3e4]              (RAM)
  018004e6:  movs r2, #1                       
  018004e8:  add.w r0, r0, r4, lsl #2          
  018004ec:  lsls r1, r2, #9                   
  018004ee:  ldr.w r0, [r0, #0x210]            
  018004f2:  bl #0x1802a1e                     -> 0x02a1e (вне списка функций)
  018004f6:  mov r0, r4                        
  018004f8:  pop.w {r4, lr}                    
  018004fc:  b.w #0x1802a28                    
  ; --- literal-пул @0x008cc (1 слов) — ВНЕ границ функции ---
  008cc:  .word 0x00202044  ; RAM
```
