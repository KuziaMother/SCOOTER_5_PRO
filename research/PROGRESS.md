# Прогресс декомпиляции: MCU- и BLE-части

**Назначение:** компактный трекер прогресса по двум образам прошивки. Детальные находки —
в [`REPORT.md`](REPORT.md) (разделы ссылками). Этот файл отвечает на вопрос
«где мы, что закрыто, где стены». Открытые задачи — в [`TODO_RESEARCH.md`](TODO_RESEARCH.md).

Обновлено: **2026-08-24** (синхронизировано с REPORT.md до **§74.1** — **MCU 99,7% декомпилировано по функциям** (678 функций), эмуляторная верификация **165/165 PASS**; §74: автономный прогон Фаза ① — `ControlLoop` warm-start harness (time-driven: реальный firmware PID+FOC на каждом tick + plant/battery/range, совпадает с `_foc_sim.py` 40/40) + архитектура boot (cold-boot blocked на `pop {pc}` trampoline / slot-table устанавливается runtime); фикс BatteryModel.discharge (float-накопление); §74.1: main-loop entry = статический потолок (RAM function-pointer dispatch — PID/FOC/scheduler/clock-init 0 стат. вызывающих; SysTick=тик; нужен live RAM-dump при reset); §71: механизм лимита скорости (RIDING_MODE {2,3,11}) + замкнутый контур 0x1d078 + модель target→км/ч (≈×20.8); §72: полная PID-динамика 0x1d078 — вся Phase B вериф. 2000/2000; §73.x–§73.11: **эмулятор моторного контура завершён** — полный набор поведенческих моделей (диагностика + TimModel PWM/такт + AdcModel токи + SpeedModel замер + MotorModel динамика + BatteryModel SoC/разряд + RangeModel запас хода), **полный автономный симул замкнутого контура сходится к target**; plant уточнён (v_max=522≈25 км/ч); live: USART3 DFU-консоль и push-кадры 'a'/'a1' подтверждены на железе; крупнейший хвост закрыт: **0x1A938 FOC-рутин** (все 5 подфункций + inline-блоки) и **0x7494 motor parameter setup** (ядро верифицировано 50/50); BLE-плейн уперся в статический потолок (§65): глубинная логика = RAM-trampoline, main-app — только SWD/XIP)

Легенда: ✅ готово · ◐ частично · ⬜ не начато · 🚧 заблокировано (нужны живые данные / железо)

---

## Сводка

| | MCU — `mcu_0007.bin` | BLE — `ble_2.7.0_0015.bin` |
|---|---|---|
| Чип | GD32/STM32F1-class, Cortex-M4F (HAL) | Realtek RTL8762C, Cortex-M4F (BLE SoC) |
| Размер | 150 841 Б | 153 890 Б |
| Шифрование | **нет** (подпись Mijia в хвосте) | APP-регион **AES-шифрован** (XIP, ключ в eFuse) |
| Функций подтверждено | **678** + 276 трамплинов | **170** (только PLAIN-регионы) |
| Справочник функций | [`functions_mcu/`](functions_mcu/README.md) | [`functions_ble/`](functions_ble/README.md) |
| Понимание архитектуры | ~85–90% («что и где») | PLAIN-регионы — 100%; APP — нечитаема из файла |
| Построчное понимание | ~1–2% всего; ключевые каналы протокола — 100% (§16.4, §23, §32, §35, §36) | н/д (APP = шифртекст) |
| Декомпилировано по функциям | **99,7%** (678 функций; 5 не-разобраных записей = артефакты/данные/dead) | PLAIN 1,9% (потолок статики §65); APP — шифртекст (SCEK/eFuse) |
| Главная стена | рантайм-RAM (вектор-таблицы в образе нет) | AES-ключ — в eFuse чипа |

---

## BLE-часть — RTL8762C (`ble_2.7.0_0015.bin`, база флеша 0x01800000)

### Регионы образа

| Регион | Размер | Статус | Примечание |
|---|---|---|---|
| OTA-заголовок `0x0` | 1 КБ | ✅ | GUID, адреса загрузки, `&release#`, `ic_type=5, xip=1, enc=1, enc_key_select=7` (§5, §6.11, §30.5) |
| Bootloader / DFU `0x400–0x3000` | ~9,7 КБ | ✅ | PLAIN-код, все функции в справочнике: CTR-заголовок, приём DFU-пакетов, BP-unlock, dual-bank, erase (§2) |
| SPI-flash драйвер + OTA `0x6000–0xa200` | ~16,9 КБ | ✅ | PLAIN-код, разобран: RDID, стирание секторов, BP-управление (§2) |
| **APP `0xa200–0x25400`** | ~111 КБ | 🚧 **AES** | энтропия 7.98–8.0, не ECB, S-box'ов в образе нет → аппаратный XIP-декрипт, ключ per-chip из eFuse (§3, §6.2, §19.2). Программная декомпиляция из .bin — **невозможна** |
| Trailer (TFOTA) `0x25400` | ~1,3 КБ | ✅ | цепочка сертификатов Mijia (ECDSA P-256), подпись; подписываемое сообщение не восстановлено (§4, §30.1–30.3, §30.7) |

### Поведение чипа на уровне протокола (не декомпиляция, но установлено)

| Что | Статус | Раздел |
|---|---|---|
| MiBeacon в рекламе — только идентификация | ✅ | §6.12.4 |
| Подписка пуша = CCCD на `0x001b`; 20 подписываемых свойств | ✅ (механизм; live-пуш не пойман) | §18 |
| Процедура bind — живой снуп, последовательность записана | ✅ | §21 |
| Чтение телеметрии: бинарный формат spec-кадра + шифрование `BleSecurityChipEncrypt` отреверсены, проверено на живом устройстве | ✅ | §6.13, §6.14 |
| SET-протокол из dex («стена последней мили» закрыта); `0x0005` = характеристика привязки | ✅ | §20.4–20.5 |
| Карта всей телеметрии (siid/piid) из плагина Mi Home | ✅ | §6.13.3, §14 |
| Соответствие siid/piid ↔ USART3 CMD 0x41..0x4B | ⬜ | §35.3 — живой снуп |

### Почему APP-регион не декомпилируется (финальный вердикт)

1. `xip=1 + enc=1` → inline-декрипт на шине при исполнении; ключ = OCEK из eFuse,
   per-chip (комбинация с UUID чипа на заводе, формула не документирована) — §6.11, §19.2.
2. UART-дамп (`rtltool read_flash`) даёт **шифртекст** — не обходит шифрование — §6.11.
3. eFuse Read на security Level 2 (рекомендация Realtek для mass production) — **Disable
   без разблокировки**; SWD, вероятно, за паролем — §19.2.
4. Рабочие методы только аппаратные: **SWD-дамп XIP-региона `0x0180E000`** на живом чипе
   (если SWD не залочен) или **захват FOTA-пакета** из Mi Home — §30.6.

---

## MCU-часть — GD32/STM32F1-class (`mcu_0007.bin`, 150 841 Б, не зашифрован)

### Общее

| Что | Статус | Раздел |
|---|---|---|
| Структура файла, карта памяти (flash @0x0/0x08000000-зеркало, RAM 128 КБ @0x20000000) | ✅ | §6.3, §26.1 |
| Загрузка: data-driven, точка входа из RAM (инжектится бутером); вектор-таблицы в образе нет | ✅ | §26.2 |
| Справочник функций: 678 подтверждённых (пролог+эпилог+BFS, пересечений 0), 41 ложных, 276 трамплинов | ✅ | `functions_mcu/` |
| NVRAM-карта (flash @0x0801F400..0x0801FFFF) | ✅ | §26.3 |
| FOTA-хвост: сертификаты Mijia, подпись (для внешнего верификатора — телефон/BLE-чип), AES-таблицы | ✅ | §30, §30.7 |
| Эмуляция (Unicorn): RX-путь и TX-механизм доказаны исполнением; широкая эмуляция от init — нет точки входа | ✅ граница определена | §16, §16.2–16.4 |

### Подсистемы и каналы

| Подсистема / канал | Статус | Детали | Раздел |
|---|---|---|---|
| **USART3 RX-путь** (фреймер → парсер) | ✅ | ISR `0x1e480` побайтовый фреймер; 2 RX-кольца: @0x881 телеметрия (`74 …`) и @0xc05 данные-канал (`51–54/5a/64 …`); CHK-валидация; карта «поле → RAM» для **всех 15 CMD** — сверено исполнением | §16.4, §35.1 |
| **USART3 TX-запросы (MCU→BLE)** | ✅ | сборщик `0x1f1cc`: **полный набор 63 CMD**, CHK = 0x63+CMD; time-sync 0x47; форвард настроек приложения (sub 0x1b/0x53) | §32 |
| **Данные-канал (кадры 0x51..0x54)** | ✅ | все **32 подкоманды** (12 set + 20 get/req) исполнением; **плюс полный статический разбор стейт-машины `0x1f71c`** — диспатч, все билдеры кадров, 3 TX-пути (кольцо @0x10b5/UART4, дескрипторы @0xa43/USART3 для `63 CMD`, RX @0xc05); ACK-кадры `65 … OK`; тихие sub 0x10/0x25 | §23, §29, **§36** |
| **Протокол «own» (рукопожатие BLE-чип↔MCU)** | ✅ (статика + исполнение) | кадры `64 6F 77 6E 20 <cmd>` = "down …": g/r/b/m/n/w; **инициатор — BLE-чип**; стейт-машина M[0x180] полностью картирована (джамп-таблица @0x1f77a, сверено Unicorn: 0→тип кадра, 1–3 SET, 4–5 GET, 0xa–0xe «down», 0x14+ события); маска @0x1565 = @0x33b ^ (u32@0x1e0+i), стейт @0x319 (0→3), флаг @0x358. **Криптография закрыта (§37):** `0x1a7ac` — 10-раундовая ОДНОНАПРАВЛЕННАЯ функция (keyed-hash/MAC), ключ = 176 Б таблицы @0x16aa (экспандер `0x1bfa0` от 16-Б сида конфига #3); «S-box'ы» @0xa907/@0xab07 — срезы кода, не биективны; ответ 'b' = f(блок), 'm'+'k' — ротация маски f(@0x1565); ключ per-device из Mi Home (бинд→NVRAM→старт 0x1ca08); **офлайн-вычисление любого ответа при известном сееде** (`crypto_own.py`) | **§36.2–36.3, §37** |
| **Blob-конфиги #1/#2/#3 + трейлер образа** | ✅ (статика + исполнение) | запись @0x844..@0x880: заголовок 7Б (`01 00 01 30 17 05 19`) + **blob #1 = ID платы `TK00323H0000001S73`** (18Б) + **blob #2 = дата-строка `00000/…`** (20Б, флаг «не задана» M[0x337]) + сид 16Б; дефолты — flash-литералы @0x1CC08/@0x1CC1C; NVRAM-карта скорректирована (@0xF864 = blob #2, не «конфиг #3»); GET 0x22/0x28/0x29/0x2a сняты (0x2a отдаёт сид raw!); RX-гейт sub 0x1a скорректирован (M[0x241]==1 блокирует). **Трейлер @0x236B8..@0x24D39** (без кодо-ссылок): u16-LUT (две вогнутые рампы) + 35-цифровая строка (build-метаданные, не регион) + манифест («…8-2023», `ETJSSZDDKJYXGSYFB2`) + **X.509-цепочка Mijia Root→Mijia Open→cert + ECDSA-подпись** (FOTA trust anchor, §30.7 конкретизирован) | **§38** |
| **Событийный канал 0x5a** | ✅ | сессионный протокол: флаги, таблица подтипов (исполнением), мастер-значение + fan-out (проверено численно) | §24 |
| **ADC / батарейный контур «сенсор → SoC %»** | ✅ | ADC ISR `0x1a31c` (стейт-машина, state-byte @0x248), state-5 снимок, замер №2 `0x1bb1c`, таск-драйвер ~`0x1cbf0`; **§40: «альтернативные режимы» разобраны** — `0x1bf24` = 3-бит железный режим (пины 6-8 порта1, входы) → дебаунс `0x1a688` + стейт-машина `0x23374` (pass-through raw) → `byte@0x268`; `0x1a894` = одноразовая boot-инициализация **TIM1 CCR** (флаг `byte@0x246` из init; CCR1/2/3 = 0→2250, BDTR bit15 — корректировка §41.5: база 0x40012C40 = CCR-подблок TIM1, не «ADC common»); `0x1e298` = re-arm DMA1 ch1 каждые ~65534 тиков (`byte@0x3C0`) + старт ADC (биты +0x18: 2/3/5); GPIO-driver `0x22000` (IDR=+0x00, конфиг +0x10..+0x2C) | §25, **§40** |
| **Физическая семантика RX-полей** | ◐ | RTC/календарь (блок 0x41) — высокая уверенность; SoC % (`0x306`, [415..535]→[0..100]) — высокая; температура °C (`0x30c`) — средне-высокая. **C42/C43/C45/C46 = GET-пейлоады sub 0x23/0x24/0x26/0x27 (протокольный смысл закрыт, §36.4)**; **C48/C49/C4A: потребителей в MCU нет, MCU опрашивает их on-demand (`63 48/49/4A`)** — физ. единицы остаются за живым снупом | §22, **§36.4** |
| **TX-планировщики** | ✅ | `0x1f600` (USART3, дескрипторы @0xa43 шаг 150 Б) + TX-кольцо `0x216e4` (UART4); push `61 30` — единственный подтип 0x30, уходит по UART4; полевая карта push (пролог 0x211f8) | §28, §33, §34 |
| **Мотор (TIM1)** | ✅ (A1 закрыт, §41) | **чип не vanilla F1** (таймеры +0x10: CCR=+0x44.., BDTR=+0x54; кастомный GPIO-блок 0x48xxxxxx; NVIC >96 IRQ); TIM1 = комплементарный 3-фазный мост (CCR4=2249 в калибровке — корректировка «BDTR=2249» §39), init `0x1d640`; **полная цепочка** `0x0799c` (регулятор: mode=i16(byte@RAM[0xFC8])←NVRAM-blob @RAM+0x4A, lerp u32-таблицы @0x19E98/@0x19FD8, signed-overflow div) → slot-3 state-machine (TBB @0x7AA4) → **slew `0xe408`** → u16@RAM[0x1357] → **duty% = byte@RAM[0xFD3]**; потребители duty% — только телеметрия/мониторы (статус-кадры BLE, пороги ==100%/>30%). **Вердикт: в образе НЕТ runtime-применения duty к CCR** (полный скан str по 5 таймерам + эмуляция 30 итераций × ветки = 0 периф-записей); регулятор вырожден: выход всегда ≈ -275 → duty 0%. Единственные CCR-записи: boot (CCR=2250, `0x1A894`) и калибровка (CCR=2/2249, `0x1CD0A`). Физический привод моторных FET — вне образа (B-класс) | **§39, §41** |
| **Калибровочные таблицы батареи** | ◐ | блок кривых @0x18036 (1124 Б, 21 кривая) найден; КОД, читающий таблицы, — не найден (§12.1); конфиг #3 (S-box шифр + ключи) расшифрован исполнением | §12, §27 |
| **OTA / FLASH self-write** | ✅ | `0x06230` программирование, `0x11cb4` init; pipeline обновления MCU в Mi Home отреверсен (MeshDfuManager: порядок, switchFirmware, pollMcuVersion, тайминги); **§42: формат FOTA-подписи расшифрован — ECDSA-SHA256 (P-256) от SHA-256(контента до PEM), ключом leaf «cert» (цепочка Mijia Open→cert валидна); ОБА реальных пакета из Mi Home верифицированы офлайн** (`verify_fota.py`) | §6.4, §11, **§42** |
| **Регион-проверка по SN** | 🚧 | таблица региональных префиксов модели в MCU найдена; код — вычисляемый указатель (read-taint = 0); на 4 Pro регион→скорость энфорсится в контроллере (аналогия) | §15, §17 |
| **Режим X / PERFORMANCE** | ◐ | признаки невыпущенного режима найдены; порогов скорости в MCU не нашёл | §13 |

### Стены MCU (почему дальше из файла не уйти)

- Вектор-таблицы/точки входа в OTA-образе нет → широкая эмуляция от init невозможна — §16.3.
- Многие задачи вызываются по RAM-указателям (data-driven, §26.2) → статически не находятся
  писатели отдельных полей (u16@0x32e/0x330 — CPU-писателя в образе нет, §26.4).
- Рантайм-значения (таблица команд @0x20000A43, blob-конфиги, счётчики) — только живой снуп / SWD-RAM.

---

## Сквозное: FOTA / APK / плагин Mi Home

| Что | Статус | Раздел |
|---|---|---|
| Pipeline обновления MCU в Mi Home (MeshDfuManager) | ✅ | §11 |
| Сертификатная цепочка Mijia — извлечена, публичные ключи cert0/cert1 идентичны в обоих образах | ✅ | §30.2 |
| Проверка ECDSA-подписи | ◐ частично — подписываемое сообщение (композитный манифест) не восстановлено; закрытие только живым FOTA-захватом | §30.3, §30.7 |
| Плагин Mi Home (JS-бандл, без нативного кода) | ✅ полная карта телеметрии `modules/10724.js`, транспорт 0x001a/0x001b, каналы 6/7 | §6.13, §14 |
| Повторный разбор APK + плагина | ✅ ключа прошивки в плагине нет; SET-протокол отреверсен; LED/button `0x0101/0x0102` на 5 Pro отсутствуют | §20 |
| Кросс-референс 4 Pro (SWD-раскладка STM32F1, RDP включён, регион в контроллере) | ✅ как аналогия предыдущего поколения | §17 |

---

## История прогресса (майлстоуны)

| Дата | Событие |
|---|---|
| 2026-08-13 | Телеметрия прочитана на живом устройстве (§6.14); полный дамп `tools/dump_telemetry.py`; единицы установлены проверками |
| 2026-08-15 | RTL8762C: официальная flash-раскладка + Security Mechanism Guide → AES-стена закрыта (ключ per-chip в eFuse, Level 2) (§19, §19.1, §19.2); CCCD-подписка (§18) |
| 2026-08-16 | Повторный разбор APK/плагина: SET-протокол, `0x0005` (§20); первый живой снуп bind (§21) |
| 2026-08-17 | RX-путь MCU полностью разобран и доказан исполнением: фреймер, формат кадров, диспетчер, карта «поле → RAM» для всех 15 CMD (§16.4) |
| 2026-08-18 | Физическая семантика RX-полей (§22); данные-канал: 32 подкоманды (§23); событийный канал 0x5a (§24); ADC-контур (§25); структура образа + NVRAM (§26); конфиг #3 с S-box (§27); TX-планировщики (§28); FOTA-инфраструктура (§30); сводный референс (§31); 63 CMD MCU→BLE (§32); push `61` по UART4 (§33–34); двунаправленная таблица + слой siid/piid (§35) |
| 2026-08-23 | Статический RE данных-канала до уровня инструкций: полный диспатч sub, все билдеры кадров, 3 TX-пути; **найден и разобран протокол рукопожатия «own»** (новый слой BLE-чип↔MCU) со шифроблоком S-box-семейства; закрыты A3/A4/A5: C42–C46 = GET-пейлоады, C48–C4A = мёртвое хранилище (on-demand опрос), писатель byte@0x358 = хвост «ok»-обмена; читатель get-0x36 = sub 0x36 {u16@0x32e/@0x330} (§36) |
| 2026-08-23 | **Криптография «own» и конфиг #3 закрыты (N3, §37):** полный алгоритм экспандера `0x1bfa0` (16→176 Б, не 48) и шифроблока `0x1a7ac` (10 раундов), всё сверено исполнением (Unicorn); «S-box'ы» @0xa907/@0xab07 — срезы кода образа (не биективны) → функция ОДНОНАПРАВЛЕННАЯ (доказано коллизиями 11k/65k); T (`0x1dd8c`) обратим (ранг 32/32); пересборка протокола 'b'/'m' с 4 корректировками §36.2; ключ per-device из Mi Home (бинд→NVRAM→старт 0x1ca08); офлайн-вычисление ответов MCU при известном сееде (`crypto_own.py`) |
| 2026-08-23 | **Blob-конфиги #1/#2/#3 + трейлер образа (§38):** запись @0x844..@0x880 = заголовок 7Б + ID платы `TK00323H0000001S73` + дата-строка (флаг M[0x337]) + сид 16Б; дефолты из flash @0x1CC08/@0x1CC1C; GET 0x22/0x28/0x29/0x2a сняты; трейлер @0x236B8..@0x24D39 = u16-LUT + 35 цифр + манифест + **X.509 Mijia Root→Mijia Open→cert + ECDSA** (FOTA trust anchor, ключи в tmp_certs/) |
| 2026-08-23 | **Моторный контур (§39):** чип не vanilla F1 (таймеры +0x10, кастомный GPIO-блок, NVIC >96); TIM1 = 3-фазный комплементарный мост (BDTR=2249); центральный стейт @0x1384; цепочка управления `0x0799c`→slew-limiter `0xe408` с обратной связью в round-robin-диспетчере `0xE658` (TBB, гейты RAM[0xA49]/RAM[0x35]) — подтверждено исполнением; применение duty к CCR не найдено (предел статического метода) |
| 2026-08-23 | **«Альтернативные ADC-режимы» разобраны (§40):** `0x1bf24` = 3-бит железный режим (пины 6-8 порта1) + подсистема режима (`0x1a688` дебаунс, `0x23374` стейт-машина с pass-through, `byte@0x268`); `0x1a894` = одноразовая boot-калибровка ADC (флаг из init `0x23480`); `0x1e298` = периодический re-arm DMA1 ch1 + старт ADC; GPIO-driver `0x22000` и layout порт-блока картированы |
| 2026-08-24 | **A1 закрыт: полная цепочка управления мотором + вердикт (§41):** регулятор `0x799c` (mode = i16(byte@RAM[0xFC8]) ← копия NVRAM-blob @RAM+0x4A, писатель `0x1093C`; lerp — u32-таблицы @0x19E98/@0x19FD8; signed-overflow div 0x162CE) → slot-3 state-machine (TBB @0x7AA4, PC=(addr+4)&~3!) → slew `0xE408` → u16@RAM[0x1357] → duty% = byte@RAM[0xFD3]; потребители — только телеметрия (статус-кадры BLE) и мониторы; **runtime-записей в CCR нет** (скан + эмуляция: 0 периф-записей), регулятор вырожден (выход всегда ≈ -275 → duty 0%); единственные CCR-записи — boot (2250) и калибровка (2/2249); корректировки §39/§40: TBB-конвенция, `0x1A894` = boot-CCR-init (не ADC), «BDTR=2249» = CCR4 |
| 2026-08-24 | **FOTA-подпись расшифрована, A3 закрыт (§42):** формат пакета = контент + PEM-цепочка (Mijia Open→cert, P-256) + ECDSA-SHA256 DER; подписано SHA-256(всё до первого BEGIN); «ненормальный DER 03 ff» (§38.5) — ошибка парсинга (байты внутри r); **оба реальных пакета из Mi Home верифицированы офлайн** (MCU d[:0x24845], BLE d[:0x2542D]); FOTA-пакет MCU = mcu_0007.bin побайтово; общий блок «MI\xefTFOTA»+`xiaomi.scooter.5pro`; B1 фактически закрыт статикой (верификатор `verify_fota.py`) |
| 2026-08-24 | **A2 закрыт (отрицательно) + корректировка §6.11 (§43):** полного SDK RTL8762C (424 МБ) + MP Tool v1.1.2.4 просмотрены: дефолтного OTA AES-ключа нет (T_ENC_KEY_SELECT: SCEK/OCEK из eFuse, +CONST в boot ROM, ON_FLASH=dec_key[16] в хедере — у Dreame нули); OCEK per-chip с линии (XML data_protection/key_gen). Точный decode BLE-хедера: файл = 2 изображения — RomPatch@0x0 (xip=0, enc=1, **select=1=SCEK_WITH_RTKCONST**, exe_base=RAM 0x203800) + AppPatch@0xA000 (xip=1, enc=0, select=0=SCEK — но payload на 100% шифр); «select=7» §6.11 — ошибка битов; шифр: RomPatch-load 11776 Б @file 0x3000 + AppPatch ~111 КБ @0xA400..0x25400; SWD-план уточнён (RAM-дамп @0x203800 даёт RomPatch-load без ключа) |
| 2026-08-24 | **Mijia Root CA найден в APK Mi Home (§44):** `assets/MijiaRootCert.der` — self-signed `O=Mijia Root, C=CN`, P-256, 2016..2066 (50 лет), SKI = AKI c0 (`96b7a27c...`) ✓; полная цепочка Root→Mijia Open→cert верифицирована (openssl OK + ручная проверка ключом root); `verify_fota.py` теперь проверяет FOTA-пакеты до trust anchor (оба пакета OK). Класс A статических задач завершён. Заметка: Python cryptography 50.0 не парсит этот DER (Rust-парсер), ключ извлекается вручную |
| 2026-08-24 | **Живая BLE-сессия (§45):** свип свойств 88/88 (живые значения: батарея 90%, пробег 8.1 км, SN `605**/CSALQM5U****` (маскирован) — префикс = одна из групп build-строки @0x24164; FW 2.7.0_0015.0007 = наши образы); SIID_6 LOG = журнал поездок (декод по плагину); **SET верифицирован live** (TCS/свет/режим с read-back); **набор режимов {2,3,0x0B} подтверждён** (byte@0x229, §34.2); **пуши: ноль на app-SET** — устройство пушит только автономные события, захват требует езды; сид sub 0x2A не тригурируется стандартными чтениями → приоритет класса C. Новые инструменты: `probes/prop_sweep.py`, `probes/push_capture.py` |
| 2026-08-24 | **Карты декомпиляции по функциям (§46):** `research/MCU_MAP.md` + `research/BLE_MAP.md` (генератор `scripts/gen_maps.py` поверх каталога функций 678/170 из functions_*). MCU: 94,7 КБ кода в 678 функциях, **декомпилировано 21,8%** (28 разобрано = 18,0% байт, 10 частично, 2 ID; было 19,5% до §47); ручная перепроверка: таблица 1:1 с каноном, входы именованных функций сверены с дизассембляцией, найден 1 артефакт детекции (0x1a5e4/0x1a5e6 «own»-трамплин); ключевые подсистемы закрыты, крупнейшие не разобранные: 0x178c4/0x0f1ec/0x06fc0. BLE: 79,2% файла — шифр SCEK (потолок без SWD ~21%), PLAIN-код 170 функций — семантически 0% |
| 2026-08-24 | **Мелкие функции ≤12 Б: пакетный разбор 66 штук (§48); карта: 21,8% → 22,2%** (67 разобрано / 30 ID). Найдено: FLASH-unlock (magic 0x45670123/0xCDEF89AB → FLASH_KEYR @0x40022004, `0x6378`), сброс FLASH_SR (`0x61d4`, caller OTA-код 0x06230), RCC-прескалер (`0xc894`), кластер 0x99xx = драйвер периферии @0x40003000 (адрес вне карты F1-class; magic 0xAAAA/0xCCCC), кластер 0x8xxx = one-time init «GPIOA + SPI1-команда 0xB9» под флагом byte@0xC8D, setters USART3/UART4+4 из TX-колец, ADC1+0x18 |= 8/0x20 из ADC-задач. **Целостность карты (§48.5):** канонический детектор пропустил гигантскую функцию 0x17xxx — дыра 0x177DE..0x19A1C ≈ 8,5 КБ (u16-таблицы + кривая @0x17BF4 + cold-path'и; dead-фрагмент 0x178c4 с невалидной инструкцией 0x6EF5 — Unicorn UC_ERR_INSN_INVALID). Новые A-задачи: гигантская функция, карта регистров периферии |
| 2026-08-24 | **Телеметрический канал MCU→BLE расшифрован (§47):** сборщик `0x211f8` (1246 Б, без вызовов) пишет в TX-кольцо @0x10b5 (слот 150 Б) кадры 'a0' (телеметрия: режимы @0x228/0x229, bias(i16@0x31c/0x290), статус-коды, u16@0x236 BE, checksum+0x9E) и 'a1' (батарея: %@0x306, T°@0x30c, u16@0x308/0x30a/0x30e); state-машина byte@0x18A: 0→1→2→0; рейт-лимиты 1920/3200 тиков (u64@0x1e0); гейт byte@0x339|0x358 → подавление телеметрии при переключении TX-кольца. MCU-сторона пушей закрыта — GATT-пуши (§45) формируются BLE-чипом из этих кадров; карта §46: 19,5%→21,8% |
| 2026-08-24 | **Полка 13–64 Б закрыта полностью: 267 функций, 9 444 Б (§49); карта MCU: 22,2% → 32,0%.** Главные находки: **I2C2 shadow-регистровая семья** (22 setter'а RAM @0x15FA..0x164C → регистры чипа на I2C2 0x40005800, dev 0x3E; чтение 0x1bdc/0x1c1c — главный канал MCU→внешний чип); **WWDG @0x40002800**; **DMA1 целиком** (инициал 0x1940/0x16d4/0x175c, конфиг канала 0x18fc + таблица @0x1A8D0, INTFR-хендлеры 0x4fc0/0x5000); **программный сброс 0x1e3a4** (SCB_AIRCR=0x5FA0004); SysTick init/delay; **CRC-семья из 7 реализаций** (CRC-32 для агрегатора, CRC-16 ×4, CRC-8, CRC-7 — верификация EEPROM); BCD-пара; критсекции @0xB5C; event-очередь @0x164C; one-shot флаги @0x8C; **OTA-команда 0xddc4** (магия 0x5AA5, коды 0xE0/0xE2, erase @0x1D800); ADC→мВ 0x8b58/0x8b10; clamp |v|≤0xC8 0xe704 (из slot-3 §41); flash-write NVRAM 0x2d5c (26B @0x1A550); мега-инициалы 0x1df4 (15 bl) / 0x11998 (10 bl); varargs 0xded4/0x307c; TBB с повреждённой таблицей 0x129b4 (новый экземпляр класса обфускации cf. 0xA907). Дальше — только функции ≥68 Б и гигантская 0x17xxx |
| 2026-08-24 | **Полка 65–128 Б закрыта полностью: 141 функция, 12 518 Б (§50); карта MCU: 32,0% → 44,8%.** Главные находки: **база NVIC = 0xE000E400** (не стандартная!; enable-helper 0xc0b4, IRQ3-reg 0x58b0, SysTick LOAD/VAL/CTRL = 0xE000E014/18/10); **FLASH @0x40022000 целиком** (erase core 0x6230, program 0x2360c, bank select + CRC-32 0x57a0, OTA-validator 0x80ac); **WWDG расшифрован** (декодеры CFR/CR с BCD 0xcc1c/0xccbc, window wait 0xce70, refresh 0xcd0c, mode set 0x14958 — WWDG = таймер событий с BCD-окном); I2C2: **CRC-7 stream-кодер** 0x9048/0x15640, batch write 0x280c, charger detection 0x3668 (reg 0x9335); **четыре NVRAM-зоны верификации** (@0xC9C/0x304C/0x30CF/0x305C + CRC-16/8, GPIO-setup 0x3588 для NVRAM-чипа); DMA1 ch1 = ADC-DMA (0x1670), ch9 init 0x1c1ac; **system sleep** 0xc2a8 (SCB_SCR SLEEPDEEP + WFI/WFE); boot clock init 0x310c (WWDG включается на старте); **HSE = 20 МГц** (0xc708: freq = div × 0x7A1200); **SPI1 = 0x40013000** (init 0x10788, read-3B 0x83e4 из one-time init 0x82b8); float-lookup'и 0x1a638/0x1de0c/0x1df84 + интерполяция core 0x1de5e; 2D fixed-point rotate 0x1d818 (мотор-регион); медиан-фильтр 0x170c + расчёт напряжения 0x3780; RLE-декодеры 0x152a/0x1a24c; TLV+CRC 0x38ec; timer IRQ handler @0x40012C00 (0x1a2a4); flash-лог сканеры 0xa8c4/0xa910 (счётчик OTA-секторов). Дальше — только функции ≥132 Б и гигантская 0x17xxx |
| 2026-08-24 | **Эмуляторная верификация каталога (§50.7/§50.7.1/§50.7.2): PASS 86/86** (скрипт `scripts/mcu/func_verify.py` — реальный код на Unicorn + Python-референс, 30 итераций/тест; покрытие «разобран»: 22 → 86 из 479 = 18%). Батч 1 (математика/CRC/BCD): **0xaad0 = CRC-16/CCITT-FALSE полином 0x1021** (не 0xA001!), 0x3c7c — XOR 0x07, 0x172b8 — [r2]=HIGH/[r3]=LOW, 0x1712c/0x17150/0x1494. Батч 2 (accessors/set-cleared/memset/RCC/FLASH): **0x4f38 — асимметрия** (clear трюнирует u32 до u16), 0x99bc — r1 = входное значение, 0xe6ec/0x593c переопределены; подтверждены RCC=0x40021000 и FLASH=0x40022000. Батч 3 (fixed-address getter'ы, RCC-семья, wait-циклы, драйвер @0x40003000): **0x21c0c/0x8a44 — фиксированные адреса** (RAM[0x2C]/RAM[0xF6A], не двойная индирекция), **wait-функции: 0xc8dc ждёт CTLR bit1=HSERDY** (не +0x24!), 0xc914 — +0x24 bit3 (не CTLR!), 0xc8a4 — CTLR bit17; 0x10abc failure-path: fallback 0x003D0900 → RAM[0xB88]; 0xc5b0 маска CFGR0=0xF7C0FFFF. Найден **баг Unicorn 2.1.4**: register-shift при cnt≥256 с (cnt&0xFF)≤28 даёт V>>(cnt&0x1F) вместо 0; исправлены баги харнеса (кумулятивный insn, биты CPSR N=31/Z=30/C=29/V=28) |
| 2026-08-24 | **Эмуляторная верификация: батч 4 (§50.7.3), итого PASS 98/98** (покрытие «разобран»: 98 из 481 = 20,4%). Находки: **set-then-clear = вынужденный сброс бита** — паттерн «clock OFF»: 0x97f4 (I2C: base==I2C1 → RCC+0x10 &= ~(1<<21), иначе ~(1<<22)), 0x17f4 (DMA1: RCC+0x28 &= ~0x1000); **0x21b84 — запись поля [7:6] байта (r0>>2)** в NVIC-блок @0xE000E400 (r0≥0) или SCB-зону @0xE000ED00+off(r0&0xF)+0x1C (r0<0; off только от младшего ниббла!); **busy-delay 0x22a0c/0x229d4: порог = 0x1000000 − coeff·N** (коэфф. RAM[0x10]/RAM[0x24]); **0x8588 AFIO remap: поле [4p+1:4p], p=arg1&3, idx=arg1>>2**, arg0 не маскируется; 0x2bbc — GPIO-порт A/B/C/D = 0/1/2/3 (общий return 0x2BEE). **Артефакт детектора:** gap 0xC63E..0xC684 = две пропущенные функции 0xC644 (RCC+0x28) / 0xC664 (RCC+0x1C) + пулы. Корректировок каталога: 7 + 2 новые записи |
| 2026-08-24 | **Полка 132–256 Б закрыта полностью: 98 функций, 17 840 Б (§51); карта MCU: 44,8% → 62,9%** (+18.1 pp — крупнейший прирост). Главные находки: **BLE-кадровая фабрика** (builders 0xd39c/0xd46c/0xd534/0xf14c + senders 0x11674.. с retry×3 0xab0c, checksum 0x8a50, send 0xaccc; voltage-пакеры mV±2000); **PB15 latch с debounce-гистерезисом** 0xfdac (ON v≥1 после 5264 тиков / OFF v≤70 после 17936) и 0xfe74 (инверсный: ON v≤5 после 224 / OFF v≥32 после 17921); **PLL setup 0x10ba0** (10 MHz HSE ref, wait PLLRDY) + **свитч на HSE/PLL 0x11cb4** (полный CFGR0/CFGR1); **WWDG window timer 0xcd80** (BCD + magic 0xCA/0x53 → +0x24, CFR&0xFFFF3F); **threshold-счётчики ×6** с flash-калибровкой @0x19DAA/0x19E1C; **интерполяторы Q31/Q8** 0x16880 (signed-ветка)/0x16938 (unsigned-ветка)/0x167b6 — args (v, xtab, ytab, last_idx=n−1), v≥конец → полный шаг к ys[n−1]; **throttle/duty** 0x1c234 (Q16) + 0x1d330 (duty shaping, все сравнения v — UNSIGNED); **drift detector 0xe740** (адаптивный базлайн при dev>500); **TIM DIER setter 0x22c70 с assert `cpsid i`** (первый зафиксированный assert-паттерн); i16×i16→u64 0x17170/0x17214; u64↔double 0x16040 (VFP); **5 gap-функций** (§51.1: getters @RAM+0x1384, MSB-normalization 0x21b24, varargs trampoline 0x21b52, NVIC ISER0 0x21b6c). **Эмуляторная верификация батч 5 (§51.2): +11 тестов, итого 109/109 PASS** (покрытие «разобран»: 109/582 = 18,7%); корректировок каталога: 7 (направления bgt/blt/bhi/ble, конвенции выходов, debounce-пороги, unsigned-сравнения) |
| 2026-08-24 | **Полка 257–512 Б закрыта полностью: 38 функций, 13 026 Б (§52); карта MCU: 62,9% → 76,2%** (+13.3 pp). Главные находки: **2D-билинейная интерполяция** 0x16bd4 (u16-сетка, stride из caller-стека, Q16) + twin 0x16d8e (u32/Q8) — эмуляторно верифицирована; **u64 Q20-арифметика** 0x19c58/0x1346 (моторная цепь 0x22274, 0x16040); **NVRAM page-reader'ы I2C2** 0x14802 (@0x30CF, 32B-страницы, окно 0x41000..0x45000) + 0x157e0 (@0x304C/0x305C, 40B) — внешний чип имеет memory-mapped EEPROM-регионы; **I2C bus recovery** 0xb302 (9 тактов SCL) + **GPIO configurator** 0x85c8 (descriptor-driven: MODER/CRL/CRH/AFR/ODR/PUPDR+EXTI); **I2C2 block-manager** 0x119e4 (2KB-буфер @RAM+0x27D4); **BLE-сессии**: state machine 0x6ccc (+twin'ы 0x6e50/0x6838, clamp 0xea64, reset 0xec70) — handshake → 0xe200 (интерполятор §51); **power/shutdown SM** 0x110fc (NVRAM-save 0x5c9c, boot-счётчики @RAM+0x8/0xC); **OTA metadata logger** 0xacce (8 слотов ×8B в @0x1F000/0x1F800, CRC-16, wear-counter); **TIM**: validated setter 0x22000 (assert cpsid i — третий паттерн), init 0x1d640, dispatcher 0x23040, capture 0x1a688. Артефакт: 0x244d2 = push в data-регионе (не код). **Эмуляторная верификация батч 6 (§52.1): +2 теста (0x161EA = точное floor((a/b)×2ⁿ), 0x16BD4), итого 111/111 PASS** |
| 2026-08-24 | **Полка 513–1000 Б закрыта полностью: 17 функций, 11 378 Б (§53); карта MCU: 76,2% → 87,6%** (+11.4 pp). Главные находки: **3D-трилинейная интерполяция** 0x16588 (u16-сетка, stride'ы {1,syy,szz}, Q16) — эмуляторно верифицирована; **clock-governor** 0x22274 (Q20-цепь → системная частота, FLASH ACR по порогам 25/49/73 МГц, AHBPRESC mode/duty-коды) + **PLL SM** 0x225f4 (bounded wait по tick RAM+0x2C, spin на ready-битах RCC+0x60); **GPIO batch configurator** 0x0332c (17 callers!); **BLE-сессии**: 0x69e4 (caller 0x16bd4 с сеткой @0x19F60 + SM), 0x70d8/0x63b8/0x6618/0xe808; **телеметрия**: 0xd938 (флагпакер: ~30 бит из ~12 struct'ов → u32 @+0x20 + checksum), 0xd00c (+OTA logger 0xaccc×2), 0x3f00 (48B-кадр); **мотор**: 0x1ce38 (TIM1 CCR4 handler, константы 3000/6000/−10000), 0x1d078 (struct @RAM+0x1768). Аномалия: hdr-слова таблиц 0x19F60/0x19F58 не декодируются как счётчики (версия данных ≠ кода?). **Эмуляторная верификация батч 7 (§53.1): +1 тест (0x16588), итого 112/112 PASS.** **§53.2 — методическая находка: capstone с нечётным стартом (off|1) сдвигает ВСЕ адреса и bl-цели на +1** (mcu_annot.py корректен; исторический каталог не затронут) |
| 2026-08-24 | **Полка ≥1001 Б закрыта: 5 функций, 6 980 Б (§54); карта MCU: 87,6% → 94,8%** (+7.2 pp). **ВСЕ 678 функций карты классифицированы — «не начат» = 0.** Найден **многократный round-robin планировщик** (0x123C0..0x12A70): цепочки gated-task thunks (enable/mode-гейты на байтах RAM) + планировщик 0x129B4 с тремя счётчиками (mod 5/A/A) и тремя TBB-таблицами по 10 слотов — «сердце» периодической работы MCU. Разбор: **0x14f50** — телеметрии-диспетчер (state byte[RAM+0x80], ADC-сэмплинг → буфер RAM+0x3024, flag-циклы через 0xd938 §53); **0x1337c** — сенсорный сэмплер + bit-pack (0x8938 §52, ×10, ~15 бит → u16[out+7]); **0x11de8** — boot/startup SM (5 states, TBB, tick-пороги 3000/500/12000, GPIO-check 0x87c8 §52); **0x07494** — моторный ramp-контроллер (сенсоры clamp [1000..5000], Q31 sentinel ±0x80000000, **ramp 0xf010/0xf024 шагом 8/тик**, 0x16fde ×2); **0x1c34c** — Q20 position/velocity controller (0x19994/0x19968 §52, clamp 32760, пороги 30/75, callers не найдены). Мелкие: **векторная таблица @0x1000** (SP=0x20004E40, reset=0x315C); 0xf010/0xf024 в каталоге |
| 2026-08-24 | **Дыра 0x174F2..0x19A1C разобрана (§55): «гигантская функция» §48.5 опровергнута — это регион данных (~9 KB) + 6 малых функций.** Код: 0x174FC (boot-сброс счётчиков), 0x17524 (boot-wait ≥4320 тиков), 0x17554 (mode-setter), **0x19968 (u32/u32 floor-деление — НЕ Q20, коррекция §52/§53)**, 0x19994 (C-style signed, truncation), 0x199BC (u64/u64, 4 аргумента!) — все три делятеля **эмуляторно верифицированы** (батч 8). Данные: версия "A06120108", float-константы 4.055/16.094, перестановочная таблица, кривая 716→335, u16-таблицы ~3000–3500, **DFU-команды "down rd_info/get_ver/dfu_active/nvm_write/wr_info/dfu_verify"** (текстовый FOTA-протокол), **строка модели "SZMC-ES-ZM-0283M"**. Методич. (§55.2): Run.call не сбрасывает R4–R12 (тест обязан передавать все аргументы); Unicorn 2.1.4: reg_read(13)≠SP; идиома «lr-on-stack». **Эмуляторная верификация: 115/115 PASS.** Неучтённого кода не осталось |
| 2026-08-24 | **hdr-аномалия §53 РАЗРЕШЕНА (§56): латентный баг, недостижимый путь.** Коррекция фрейм-анализа: `bl` НЕ пушит lr (callee SP = caller SP) → r6=ygrid, sb=hdr-указатель в 0x16BD4. Механизм: hdr-слова u32[0x19F60]=0x05014674 читаются ТОЛЬКО при v1>xtab[0]=2002, но caller 0x69E4 передаёт v1=s8∈[−128..127] → всегда early-exit, результат = константа grid[0]=0x3A08 (эмуляторно подтверждено). xtab {2002..63734} — кривая по полному u16-диапазону под другой вызывающий. **Батч 9: +5 тестов — 120/120 PASS** (0xF010/0xF024 window-gate'ы — коррекция §54 «ramp», 0x8E50/0x8E70 cfg×10 с игнорированием аргумента, 0x16BD4 early-exit). Методич.: Unicorn 2.1.4 R0–R9 = id 66–75, только константы arm_const |
| 2026-08-24 | **0xE658 полностью расшифрован (§57): вторичный round-robin диспетчер группы BLE/моторных задач.** Гейты: byte[RAM+0x35]∈{0,1,2} && byte[RAM+0xA49]==1; безусловный bl 0x6618 (BLE SM #7); dispatch @0xE684 = **vendor-инструкция** (байты df e8 — невалидный TBB по стандарту ARM; capstone misdecode, Unicorn реализует семантику чипа: target = 0xE69A + 6×r0); 10 слотов: {0x6E50, 0x63B8, 0x799C, 0x7A30, 0x69E4, 0x6838} + 4 пустых; counter byte[RAM+0xA62] mod 10 (wrap 9→0); мутационный тест: байты 0xE688..0xE691 не влияют на dispatch (padding). **Найден баг**: задача 0x69E4 (slot 4) вызывает 0x16F42 → OOB-чтение на текущих flash-таблицах (u32[hdr+4]=0x98001053 как индекс) → fault @0x16FA2, counter застревает. Методич.: Run.call глотал UcError (fault маскировался как чистый возврат) — добавлен run.last_error. **Батч 10: +1 тест — 121/121 PASS** |
| 2026-08-24 | **ADC1-кластер разобран (§58): последний чистый код из «ID» закрыт.** 0x21CA8 (validated channel-configurator: 7 asserts, SMPR1/SMPR2/+0x40/+0x7C поля, SWSTART→reset→ADON), 0x1C0B0 (sensor-init: ch A/B/C/F rank 1-4 smp=4 + финальные OR + ADON), 0x21FB8 (reset-helper: magic 0x0FFF0000 в +0x3C — не в карте функций, артефакт детектора), 0x21E18 (channel-sequencer: SQR rank-регистры, sampling-поля, SMPR1 bit25). Раскладка ADC1 @0x40012400 подтверждена исполнением (+0x10 layout). **Методич. (§58.4): raw-shift ≥32→0 (отклонение от ARMv7-M — sampling достижим только ch0..6, ch7-10 компенсирует caller); `bx lr` с вручную записанным LR «сквозит» (билд Unicorn); scratch-код вне оригинальных адресов ненадёжен.** **Батч 11: +4 теста — 125/125 PASS; карта MCU 95,3%** |
| 2026-08-24 | **Четыре «частично»-функции разобраны и верифицированы (§59).** 0x23374 (3-проводная шина: полная таблица решений, 48 кейсов), 0x1E2F8 (RCC/GPIOC init: последовательность битов + финал 0x100000), 0x1302C (RCC-пульсатор 5 USART: 0x40013800/0x40004400/0x40004800/0x40015000/0x40015400), 0x1E298 (DMA+ADC: validated-write 0x2359C в блок @0x40020000). **Методич. (§59.2): bls/CC = ≤ (с равенством!), bhi/HS = > строго; POP/LDM SP всегда восстанавливает PC (бит 7 игнорируется); capstone ненадёжен системно.** Обнаружена расширенная периф-область @0x48000000 (0x1BF48/0x1D640). **Батч 12: +4 теста — 129/129 PASS; «частично» 9→5; карта MCU 95,6%** |
| 2026-08-24 | **Мотор-инициализация разобрана и верифицирована (§59.5).** McuEmu: замаплена расширенная периф-область **@0x48000000** (128 K). 0x1D640 (мотор-TIM @0x40012C00: prescaler 0x8CA, CCR 0x45B→0/0x8C9, enable +0x00=0x41 последним; channel-блоки 0x48000000/0x48000400), 0x1BF48 (мотор-инит: 221 запись, GPIO-блок @0x48000C00 через validated-setter 0x22000), 0x1C1AC (**ADC-DMA канал**: src=0x40012450 (ADC1 DR), dst=0x20001692 (RAM+0x1692), ctrl=0x5D000041; IRQ: [0xE000E100]=0x200 прямая запись, NVIC+0x08 &= ~0xFF00). **Батч 13: +3 теста — 132/132 PASS; «частично» 5→4** |
| 2026-08-24 | **RX-парсер USART3 0x1E9E0 разобран и верифицирован (§59.6).** Вызывающий — периодический таск 0x1DFD8 (bl @0x1E0AE; в firmware нет ни одного blx/bx reg). Контракт: state=byte[RAM+0x172], head=byte[RAM+0x2C0] (кольцо 3 слота × 150 B @RAM+0x881), toggle=byte[RAM+0x2C9] → context-слот @RAM+0x10B5+i*0x96. Dispatch: '@'→head++; 'G'/'K'/'c'→frame-start (len 20/27/83, CRC, checksum); 'A'→telemetry-snapshot (ring[3..9] → RAM+0x2E8..0x2F1 c rev16/нибблами/bit-сплитом ring7); 'a'/др. → response-сборщики. **Методич.: Bcond на чипе = D0xx (BEQ, imm8 в [7:0]) — не стандартный ARM.** **Батч 14: +2 теста — 134/134 PASS; «частично» 4→3; карта MCU 96,6%** |
| 2026-08-24 | **«own» round 0x1DD8C разобран и верифицирован (§59.7).** Чистая функция от буфера в r0: 16 B = 4 группы × 4 байта; каждая [A,B,C,D] смешивается независимо через CRC-8 (poly **0x1B**, MSB-first) как Feistel: X=A^B^C^D; A'=A^crc8(A^B)^X, B'=B^crc8(B^C)^X, C'=C^crc8(C^D)^X, D'=D^crc8(D^A)^X. Модель сверена с эмулятором 20/20. |
| 2026-08-24 | **Периодический таск USART3 0x1DFD8: inline-логика верифицирована (§59.8).** u16 tick @0x2BA++; 3 утечных интегратора с обратной связью new_acc=(acc+delta-out)&0xFFFFFFFF, new_out=asr(new_acc,5) s16 (пары 0x298/0x278/0x27A и др.); % батареи из level=s16[0x1794+12]: [415,535]→[0,100], (level-415)*100//120; главный u32 @0x314++. 6 bl патч в NOP (save/restore) — изоляция inline-логики. **Методич.: граница «store 0» — строгий `<` (capstone метит ble — 5-е подтверждение ненадёжности cond-меток).** **Батч 15: +2 теста — 136/136 PASS; «частично» 3→1; карта MCU 96,8%** |
| 2026-08-24 | **0x1A938 (последний «частично», 3296 B): структурный анализ + коррекция (§59.9).** Попытка полной верификации уперлась в сложность. **Коррекция:** подфункции все моторные (0x1be1c TIM-capture, 0x1d7ac векторная, 0x1d818 2D-rotate, 0x1bd88 quadrant) → это **FOC PMSM/BLDC**, а не «батарейные пороги» (неполное описание §6.5/§22). Батарейный аспект (@0x306, cmp 90, гейт @0x22e) — лишь одна ветка. Вход сразу делегирует в 0x1be1c; при idle — init-проход (71 запись, константы). 3 возврата = state-machine-ветки (связаны межчастными ветками — не склейка). Полная верификация — отдельный глубокий проход (след state + подфункции). Оставлен «частично» с обогащённым описанием |
| 2026-08-24 | **Глубокий проход 0x1A938: подфункция 0x1be1c верифицирована (§60).** TIM capture → FOC реконструкция фазных токов. Гейт = бит15 низких16 u32[0x40012C54]: =0→clamp+copy r0[+0xc/+0x10/+0x14]→OUT@RAM+0x838; =1→dispatch u16[r0+2] (computed-goto 0x21b52, count=7): 0/≥7→null, 1→B, 2/3→C, 4/5→A, 6→B. Хендлеры A/B/C: две фазы (T−C)<<4 из u32[0x40012440+{28,2c,30}]−u16[r0+{18,1a,1c}], третья=−сумма (Кирхгоф), clamp [−30000,30000]. 6-секторная FOC. **Батч 16: +1 тест — 137/137 PASS.** Далее: 0x1d7ac, 0x1bd88, след state-machine |
| 2026-08-24 | **Глубокий проход 0x1A938 (продолжение): 0x1e410 + 0x1d7ac верифицированы (§60.1).** 0x1e410 — табличная декодировка Q15-вектора: ветвь=A[15:14], i1=(A>>6)&0xFF, i2=~i1&0xFF, t1/t2=s16[TBL@flash0xA6C6[i]], 4-квадрантные знаковые инверсии → u32. 0x1d7ac — cross+dot двух Q15-векторов (X=0x1e410(arg1), A=arg0): lo=cross(a_hi·X_lo−a_lo·X_hi), hi=dot(a_lo·X_lo+a_hi·X_hi), каждое произведение /2^15 с округлением (p<0→+0x7FFF). **Батч 16: +2 теста — 139/139 PASS.** Глубокий проход: 3 из 4 подфункций. Далее: 0x1bd88, след state-machine |
| 2026-08-24 | **Глубокий проход 0x1A938 (завершение подфункций): 0x1bd88 верифицирован (§60.2).** 6-секторный FOC-классификатор: гейт=бит15 низких16 u32[0x40012C54] (=0→обход); x0=s16[r1+0], x2=s16[r1+2]; S=rnd((B·x2+A·x0)/2), D=rnd((B·x2−A·x0)/2) A=0x3ce4 B=0x2328; u16[r0+2]=сектор по знакам S/D/(B·x2). **ВСЕ 4 подфункции 0x1A938 верифицированы.** Находка: capstone pool-offset снова неверен (5-е подтверждение §53) — ldr r3,[pc,#0x74] реально читает 0x1be00=TIMER_A, не 0x88004808; PERIPH3 добавил и откатил. **Батч 16: +1 тест — 140/140 PASS.** Осталась трассировка state-machine основного тела |
| 2026-08-24 | **Глубокий проход 0x1A938 (продолжение): 0x1d818 верифицирован + архитектура state-machine (§60.3).** 0x1d818 = cross+dot, двойник 0x1d7ac, но X из RAM+0x10c (X_lo=s16[+0x10c], X_hi=s16[+0x10e]) — найден трассировкой (5-я подфункция, не была в §59.9). **ВСЕ 5 подфункций верифицированы.** State-machine: нулевое состояние → 458 insn, чистый возврат, поток TIM→cross+dot(→tbl)→inline-блок 0x1aa08(109insn)→условный bl 0x1ab68 (cmp r3,#0;bge)→fixed-point 0x1b3f2→0x1d818→0x1bd88. Одиночные byte-флаги путь не меняют — гейты многобайтные. Capstone декодирует лишь ~40% тела (vendor-инструкции). **Батч 16: +1 тест — 141/141 PASS.** Осталась поинструкционная верификация inline-блоков |
| 2026-08-24 | **Глубокий проход 0x1A938 (продолжение): первый inline-блок 0x1b3f2..0x1b460 верифицирован (§60.4).** inlined cross+dot (тот же комплексный произведение, что 0x1d7ac/0x1d818): P=u32[RAM+0x388], X_lo=s16[RAM+0x108], X_hi=s16[RAM+0x10a], R6=u32[RAM+0x3ac]; cross=asr15(R6·Xhi)−asr15(P·Xlo)→RAM+0x3b8, dot=asr15(R6·Xlo)+asr15(P·Xhi)→RAM+0x3bc; r4+0x1c/0x20/0x24/0x28 — производные (константы 0x6ed9/0x376d). Находки: **patch-based изоляция** (mid-function jump + r4=RAM+0x040 + stop, обходит «входы перезаписаны upstream»); capstone pool-offsets 6-й раз неверны (r6 реально u32[RAM+0x3ac], X_hi из RAM+0x10a). **Батч 16: +1 тест — 142/142 PASS.** Остались inline-блоки 0x1aa08(109insn) и 0x1b48a..0x1b5f4 |
| 2026-08-24 | **Глубокий проход 0x1A938 (продолжение): inline-блок 0x1aa08..0x1aae6 — ключевые операции верифицированы (§60.5).** Обработка/кламп моторных параметров + leaky-интегратор. Верифицировано: (1) u16[RAM+0x044]=lo16(cross+dot); (2) u16[RAM+0x38c]=clamp(−s16[RAM+0x0a6],±s16[RAM+0x390]); (3) интегратор acc=u32[RAM+0x094]: u16[RAM+0x046]=asr(acc_new,5) — тот же паттерн, что 0x1DFD8. Находки: **live-in регистры из полного запуска** (hook code на входе блока → R0..R12/SP → воспроизведение в mid-function jump; обобщение patch-based изоляции §60.4 на register-зависимые блоки); ширина чтения выходов = ширина записи блока. **Батч 16: +1 тест — 143/143 PASS.** Остался блок 0x1b48a..0x1b5f4 |
| 2026-08-24 | **Глубокий проход 0x1A938 завершён по основным блокам: inline-блок 0x1b48a..0x1b5f4 (коммутация/классификатор секторов FOC) верифицирован (§60.6).** Входы v2c/v30/v34=u32[r4+0x2c/0x30/0x34], порог 16383 (0x3fff). Верифицировано: default pass-through (все≤порога → r4+0x38=v2c,0x3c=v30,0x40=v34) + v2c-max сектор (r4+0x38=16383, r4+0x3c=min(v30−asr(v2c,1)+8192,16383), r4+0x40=r1; кламп 17192→16383). **Итог: все 5 подфункций + все 3 крупных inline-блока верифицированы** — самая глубокая функция проекта (3.5 KB) разобрана по всем основным блокам. **Батч 16: +1 тест — 144/144 PASS.** |
| 2026-08-24 | **Мелкие inline-фрагменты 0x1A938 верифицированы (§60.7, +3 теста → 147/147 PASS).** §60.7a 0x1b470: v2c/v30/v34 (входы блока 3) из r0/r1/r2 — if r0<=r2: (r1,r2−r0,−r0) else (r0−r2,r1,−r2). §60.7b 0x1b584: хвост (финальный PWM FOC): svX=asr15(4500·vX)→r4+0x44/48/4c, offset=r4+0x50=asr1(2250−max(sv)), фазы u16[RAM+0x382/384/386]=2250−(svX+offset). §60.7c 0x1a9f2: pre-block-2 frame setup ([sp+0x18]=base+0x60=RAM+0xa0). **Итог по 0x1A938: все 5 подфункций + 3 крупных блока + 3 мелких фрагмента верифицированы**; остались только тривиальные 1-2 insn зазоры + entry (вызывает верифицированный 0x1be1c). |
| 2026-08-24 | **🎯 0x1A938 ЗАКРЫТ: entry 0x1a938..0x1a9d0 верифицирован (§60.8, +1 тест → 148/148 PASS).** Табличная выборка — треугольный mapping по flash-таблице @0x9E38 (2048 u16): idx=value>>4 (value<16384) | 1023−(value−16384)>>4 (value>=16384); u16[r4+4]=idx, u16[RAM+0x108]=tableE[idx], r0=±tableE[1024−idx] (модель 200/200 по всему [0,32767]); branch C неактивен для s16. + scaled-delta u16[r4+0x62]=asr16(0x93cc·delta) (60/60). Крошечные зазоры (1-2 insn) — тривиальные store/ветки. **Статус 0x1A938: «частично» → «разобран». Карта MCU: 96,8% → 98,5%.** Находка: capstone pool-offsets 7-й раз неверны (poolC=RAM+0x838 не 0x110; таблица base 0x9E38 не 0x9E40). |
| 2026-08-24 | **Досмотр остатка карты (§61): 7 некаталогизированных функций идентифицированы + ~23 thunks повышены «ID»→«разобран». Карта MCU 98,5% → 99,7%, тесты 149/149 PASS.** Идентифицировано: **0x11bac = форматтер даты/времени (верифицирован, 100/100)** — total-seconds → 6 байт через цепочку остатков (дивизоры [31104000,2592000,86400,3600,60] ≈ 360d-год/30d-месяц/день/час/мин/сек); **0x13284/0x131fc** = state-machine контекст-слотов (близнецы, r2>0x96→return 0, mod-4 счётчик); **0x1a894** = инициализатор struct/счётчика; **0xf14c** = init/retry (цикл bl 0xab0c ≤3 до r0==1); **0x6fc0** = многоступенчатая операция (seq sub-calls); **0x2d70** = инициализатор stack-struct. Thunks (~23) — поведение «вызвать X», полностью декомпилированы. Остаток 5 записей (396 Б) — легитимно не-код: 0x244d2/0x17736/0x177d6/0x178c4 (артефакты/data/dead), 0xc6f0 (dead code). **Фактический код MCU практически полностью декомпилирован.** |
| 2026-08-24 | **§62: точная структура FOTA-пакета BLE + SCEK/XIP-модель (SDK-подтверждено).** Ключевая находка: **полный SDK Realtek RTL8762C в репо** (`zip_archives/rtl8762c-sdk-full/`) — авторитетные определения структур хедеров (`patch_header_check.h`). Декодированы (по SDK-оффсетам, скрипт `scripts/ble/fota_header_decode.py`): пакет = **два 1KB-хедера** (оба ic_type=5, ОДИН uuid `6d67def1…`) + payload'ы + cert-trailer. **HDR #1 @0x0000** (boot/ctrl image): enc=1, xip=0, enc_key_select=1 (SCEK+RTKCONST), exe_base=RAM 0x203800, load_len=11776, payload 39 КБ = bootloader/OTA (PLAINTEXT). **HDR #2 @0xa000** (main app): xip=1, enc=0 (legacy-флаги), payload_len=111496 (~110 КБ CIPHERTEXT, XIP-decrypt). Флаги enc/xip — legacy: реальное шифрование = аппаратный XIP-decrypt по OTP/eFuse-ключу. Обход статикой невозможен; путь к плейнтексту main-app = SWD-дамп XIP-региона (чеклист в §62). |
| 2026-08-24 | **§63: фундамент декомпиляции PLAIN-кода BLE** (bootloader + flash/OTA, 39 КБ — часть без дешифрования). Создан **эмулятор RTL8762C** (`emulator/ble_emu.py`, на базе mcu_emu.py; FLASH@0x01800000, RAM@0x20000000, PERIPH@0x40000000; чистые возвраты проверены). **SDK-карта периферии** (авторитетно из `rtl8762c-sdk-full/`): SPIC_FLASH=0x40080000, HW_AES=0x40014000, GPIO=0x40001000, TIM0=0x40002000. **Flash-драйвер локализован** (кластер 0x6xxx): 0x639a = SPIC-регистр accessor (возвращает SPIC+0x11c), 0x63a4 = SPIC config. Плейн-код = много trampoline-dispatch таблиц (0x2a50/0x2a78) + логика с push-прологом (топ: 0x07dea 990Б, 0x08588 846Б). **Каталог ANALYZED_BLE** + интеграция в BLE_MAP.md (статусы/% как MCU); check_maps.py адаптирован (8 колонок). Первая партия: 5 функций, PLAIN-разобрано 0.4%. Дальше — декодирование по caller-графу от хабов + эмуляторная верификация (многотурово). |
| 2026-08-24 | **§64: CTR/DFU-протокол BLE (валидация кадров) + flash-драйвер.** Декодирован **CTR length-gate `0x23f0`**: допустимые длины {5} ∪ [0x12c..0x12e] (контрольный 5Б + data-кадры 300-302Б); валидная длина → callee (0x2e24/0x2388/0x2e1a) — полная валидация = гейт+содержимое через trampoline в RAM (многотурово). **DFU event dispatcher `0x245a`**: type==4/[r3]==1 или type==2 → таймер dfuPacketWaitTimer ([base+0x1c0]×1000 → bl 0x2df2); type==4/[r3]==3 → bl 0x2e2e. **pattern-search `0x23d6`** (6-байт, state [base+0x1c9]=1/2). Flash-драйвер (SPIC@0x40080000): 0x6110/0x639a/0x63a4/0x6570/0x6a12/0x6ae6/0x7a08. Эмулятор подтвердил путь len==5 → trampoline → RAM-код 0x47c10. PLAIN-разобрано 1.2%. |
| 2026-08-24 | **§65: таксономия PLAIN-кода BLE + потолок статической декомпиляции.** Выстроена полная картина 170 функций: (1) getter/setter семейства (0x6008..0x60e4 — config-struct), (2) init/reset (0x9f80), (3) гейты/диспетчеры (CTR length-gate, DFU dispatcher — §64), (4) trampoline-stub'ы в RAM (носители глубинной логики), (5) flash-драйвер интерфейс (SPIC). **Ключевой вывод: статическая декомпиляция уперлась в потолок** — глубинные callee (CTR/DFU/flash read-write-erase) = trampoline в RAM (0x2xxxxxx/0x4xxxxxx/0x21xxxxxx), тела не в образе; SPIC-функции не «загораются» с dummy-аргументами (нужно точное рантайм-состояние). RTL8762C — несколько RAM-областей. Статикой дальше — только тривиальные setter/init (низкая отдача). Путь пробить потолок: SWD/XIP-дамп расшифрованного main-app или live-наблюдение USART3+RAM при OTA. PLAIN-разобрано 1.9%. |
| 2026-08-24 | **§66: motor parameter setup `0x7494` (1282 Б, крупнейший MCU-хвост) — РАЗОБРАН.** Это вычисление/настройка мотор-параметров (не стейт-машина). Entry: читает 3 параметра (0x8f04=константа 4200, 0x8ecc=u16[RAM+0xf9d], 0x8ee8=u16[RAM+0xf9b]) и clamp'ит каждое к [1000,5000] (на стеке); геттеры имеют side-effect в working-struct RAM+0x1359. Тело: window-gate семейство (§56) + интерполяция 0x16fde; константы 0xc80(3200)/0x7f(127). Функция завершается чисто (pop pc @0x7992), r0=0x15e802. **Ядро верифицировано 50/50:** p0=sdiv(\|val2−val3\|×(min(val2,val3)−3200), u16(1000−\|diff\|)) — критическая тонкость: знаменатель читается как **u16** (без sign-extension); [struct+0xc]=min+clamp_s16(p0). Хвост: ~15 bit-gated window-gate вызовов (config-биты → поля struct) + 2×bl 0x16fde (табличная интерполяция) + процентное r5×X/100 + clamp [5000,10000]. Working-struct=RAM+0x2fec. Переход с исчерпанного BLE-плейна к MCU-хвосту; **крупнейший MCU-хвост закрыт**. |
| 2026-08-24 | **§67+§68: fixed-point 0x1c34c.** §67 (кросс-ref на §55): подтверждено, что `0x19968`(u32 div)/`0x19994`(s32 div) — software-деление (не hardware sdiv), уже разобранное в §55; повторно верифицировано (5/5, 6/6). §68: `0x1c34c` (1244 Б) = fixed-point piecewise-linear вычисление (калибровочная кривая): entry (config-флаг → база 680/хранящаяся), линейные сегменты (output=pool×(x−30)/300+1000 для x∈[30,330)), 64-bit min/max tracking, выход — таблица 8×s32 в RAM+0x17ed8. Алгоритм+деление+сегменты разобраны; полевые коэф = калибровочные данные. |
| 2026-08-24 | **§69: закрытие дубликата 0x1d0c6 + полный разбор 0x1d078 (596 Б).** Обнаружено: 0x1d0c6 (середина функции, «адрес приблизительный», §34.2) = **устаревший дубликат** 0x1d078 (реальный старт `push {r3-r7,lr}` @0x1d078, конец `pop pc` @0x1d2ca; уже «разобран» §53). Дубликат удалён из каталога, логика свёрнута в 0x1d078. **Верифицировано 100%:** вход u32[RAM+0x158]+byte[RAM+0x100]→0x1d874 (F==0→V, else sdiv(V,30016)); 2 leaky-интегратора (>>5 / >>3 clamp≥0) в r4=RAM+0x1768; **процент** u16[RAM+0x236]=sdiv(out2×100,208) (§34.2); **mode-target** (flag=byte@0x339==1→522; mode@0x229: 0xb→125 / 2→u16@0x324 / 3→u16@0x326 / else→208; r4[+0x14]=min(target, u16[RAM+0x326])). Ловушка: 2-й `ldr [pc,#0x21c]` грузит RAM+0x326 (не 0x339 — разные pool-входы) — подтверждено трассировкой. **Бонус:** исправлен баг модели теста 0xe740 (drift detector) — firmware использует **devB=|B−refB| (абсолютное)**, а не знаковое; верифицировано 201/201. Тесты **149/149 PASS**.
| 2026-08-24 | **§70: DFU-протокол — детализация строкового пула команд @0x1893c + окружение блока данных.** Углубление §55.6 (там уже верно: текстовый DFU/FOTA). Точные оффсеты 7 команд (шаг 0x32=50B, не 48): rd_info/get_ver/dfu_active/nvm_write/wr_info/dfu_verify/ok (префикс `down`, терминатор `\x0d`) + ответы error/'ok 0000000000'/'0002'. Окружение блока @0x18900..0x19810: u16-таблица [1,2,4..256] @0x18900; 7×u32 (hi=[1,3,6,12,15,30,61]≈command-ID) @0x18920; заш. blob @0x1907b; NVM 0xFF @0x19575..0x197ff (суб-локации 0x19584/0x19684/0x19784). **Dispatch статически неразрешим**: base пула не из literal-pool (0 хитов) и не movw/movt (полный проход capstone, 0) → из RAM (runtime), потолок §65. Словарь команд извлечён полностью. Карта: каталог 0x1893C обогащён (§55/§70).
| 2026-08-24 | **§69.1: коррекция модели 0x1d078 (random-sweep поймал ошибку ручных кейсов) + перманентный тест.** При добавлении постоянного теста @t(0x1D078) random-sweep (200 наборов) вскрыл, что первоначальная модель §69 **неверна**: было `val=sdiv(V,30016)`+`min(target,maxc)` (4 ручных кейса совпали случайно: asr(92,5)=asr(90,5)=2). Реально: **val=s16(sdiv(48000,V))** при F≠0 (делитель 48000=0xbb80; 0x19994=r0/r1 → 48000/V), val=0 при F==0; кламп target — **signed** (`target if s16(target)≤u16[RAM+0x326] else u16[RAM+0x326]`, не min). После коррекции: sp0/out1/out2/pct/target = **200/200**. Перманентный тест @t(0x1D078) добавлен (в конец сьюта, чтобы не сдвигать RNG-позиции существующих — иначе вскрылся латентный баг 0xfdac). Тесты **150/150 PASS**. Урок: ручные кейсы с малыми значениями маскируют ошибки делителя/знака — random-sweep обязателен.
| 2026-08-24 | **§71: лимит скорости — механизм + target→км/ч + замкнутый контур 0x1d078 (live + эмулятор).** Ответ на «какой лимит и как выставить 35». Механизм: **лимит = RIDING_MODE (siid=1 piid=1)** дискретные {11=P ходьба, 2=D стандарт, 3=S спорт, 4=X performance(резерв)}, смена — двойным нажатием кнопки питания; live push 'a0' байт[3]=mode-nibble → сейчас **mode@0x229=3 (спорт)**. Target-значения (§69): ходьба=125, стандарт=u16@0x324(NVM), спорт=u16@0x326(NVM), max/flag=**522**(const, `ldr r1,[pc]=0x20a` НЕ чтение памяти — flash там 0xFF), else=208; кламп min_s(target,u16@0x326). **0x1d078 = замкнутый контур регулирования скорости** (не отображение): structB[+0x2a]=target=setpoint (**вериф. random-sweep 300/300**, расширен тест @t(0x1D078)); out2=оценка скорости ∝ sdiv(48000,V); вывод r4[+0x18]=[+0x64]=throttle/duty (НЕ скорость). **Модель target→км/ч**: ≈(км/ч)×20.8 (якоря: ходьба 125=6 км/ч, max 522≈25 км/ч EU); 35 км/ч→target~729. Встроенный потолок ≈25 км/ч; 35 — выше, требует записи u16@0x326 в NVM (+40%) + оговорки (device-калибровка, FOC-лимиты тока, безопасность/закон). Число км/ч эмулятором НЕ проверяется (физика). Тесты **150/150 PASS**.
| 2026-08-24 | **§72: полная PID-динамика 0x1d078 — 2 фазы + PI-регулятор + anti-windup.** Углубление §71.3. (1) **Двухфазность**: mod-2 toggle по счётчику S+0x28 — Phase A (counter=0→1, короткий путь: держит выход r4[+0x18]=S+0x64), Phase B (counter≠0→сброс 0: полный ramp-core). Объясняет, почему §71 sweep давал S+0x2c=0 (всегда Phase A). (2) **Phase B = PI-регулятор**: P-term `S+0x2c=min(err,300)` где err=(target−out2) если out1<target иначе (out2−target), `S+0x5c=s16(S+0x2c)<<7`; I-term `S+0x58+=5·err` − asr(u32[0x1764]−u32[0x388],3) → **snap [8000,8192]** (база ~8000), `S+0x60=asr(S+0x58,2)`; вывод `S+0x64=P±integral/4` кламп **[1000,32760]**. (3) **Anti-windup** гейтится u32[RAM+0x1760] (==0→сброс S+0x58/S+0x64/S+0x2e; ≠0→slew-limit через S+0x2e/u32[0x224]). Константы: 0x7d=125 → ×8=1000 (min), ×64=8000 (baseline); max 32760. **Верификация**: P-term+toggle random-sweep **400/400** (positive-target домен) → перманентный тест @t(0x1D078) §72; negative-target — вне домена. Тесты **151/151 PASS**.
| 2026-08-24 | **§72.4: полная верификация I-term + anti-windup (Phase B) 0x1d078.** Довели §72 до 100% верификации. (1) **Исправление окна интегратора**: pool[19]=0x1ffe0=**131040** (не 8192); при integral>131040 прыжок на 1d230 с r3=131040 (присваивание 8000 в 1d228-1d22a пропускается) → **clamp[8000,131040]**, а не snap-к-8000. (2) **sxth out1/out2**: после asrs идёт sxth (1d098/1d0ac) → out1_new/out2_new = s16 (не s32), out2 доп. кламп ≥0 — ключ к совпадению S64. (3) **Полная модель Phase B** (S58 интегратор, S60=integral/4, S64 вывод, S2e счётчик) с контролируемыми внешними входами u32[0x1760/0x1764/0x388/0x224]: random-sweep **2000/2000** (оба пути anti-windup: reset при u1760==0 → S58=u224×4/S64=0/S2e=0; slew при ≠0 → счётчик S2e, подвод integral/output). → 2-й перманентный тест @t(0x1D078) §72. Тесты **152/152 PASS**.
| 2026-08-24 | **§73: обновление эмулятора — периферийная диагностика + TimModel, FOC→моторный таймер.** Первый шаг «постепенного обновления эмулятора» под моторный контур. (1) **Диагностический каркас** в McuEmu: periph_writes (перехват всех записей в периферию), periph_reads/trace_periph, set_periph/apply_periph_overrides (точные значения вместо blanket 0xFF/0x00), report_periph(). (2) **Обнаружен моторный таймер** трассой полного входа FOC 0x1a938 (513 инстр., чистый возврат): base=**0x40012c00**, блок @0x1bde0 — CTRL(+0x30) RMW `&=0xDFFF` (clear бит 13), DATA0/1/2 (+0x44/48/4c) ← u16[RAM+0x386/384/382] (вычислены FOC, с нулевой RAM = 0x465, детерминизм 7/7 value), STAT(+0x54) read. (3) **TimModel** — поведенческая модель таймера (scoped-вид, реестр записей, named-доступ, snapshot(), заглушка tick() под тактовую модель). (4) Перманентный тест @t(0x1A938) §73: FOC конфигурирует таймер корректно. (5) Data-driven скан карты периферии MCU (0x40010000 86× главный таймер, 0x40012000/0x4002xxxx таймеры/PWM, 0x40004000 USART3, 0x40014000 массив/ADC). Тесты **153/153 PASS**.
| 2026-08-24 | **§73.x: тактовая модель TIM (TimModel.tick).** Реализована тактовая модель в TimModel: свободно-ходный счётчик `cnt += step` с wrap по `period`, при wrap → `update` (аналог TIM update-события); параметры period/step задаёт вызывающий (вендор-блок 0x40012xxx статически не раскрывается → модель параметризована). Плюс RMW-хелперы set/clear_status_bits. **Ключевой факт:** FOC НЕ поллит CNT — он поллит **STAT бит15** (гейт режима): =0 → обход (0 записей таймера), =1 → config (4 записи: CTRL RMW + 3 DATA). Сканы: firmware активно использует моторный блок (0x40012c40 ×13, 0x40012c00 ×12 в пуле), GPT SDK (0x40002xxx) лишь 1× → «такт» этого firmware = STAT-событие моторного блока. Тест @t(0x1A938) §73.x: tick до wrap→update → set_status_bits(STAT,бит15) → FOC переключается с обхода на config (устойчиво по seed 1/7/123). Тесты **154/154 PASS**.
| 2026-08-24 | **§73.7: ADC/capture-модель (AdcModel) — сенсоры фазных токов.** Разобран канал измерения тока FOC (0x1be1c, ADC-inject PWM): firmware читает T28/T2C/T30 (periph @0x40012468/6c/70, база 0x40012440) и реконструирует фазные токи: фаза=(T−C)<<4, третья=−сумма (Кирхгоф), clamp[−30000,30000]; C-ссылки u16[r0+0x18/0x1a/0x1c]; гейт — STAT бит15. **sector→handler** верифицирован прямым вызовом 0x1be1c на всех секторах (конвенция каталога §60): 0→null,1→B,2/3→C,4/5→A,6→B; A:T28→o_c,T2C→o_10; B:T2C→o_10,T30→o_14; C:T28→o_c,T30→o_14. Класс AdcModel: set_captures/get_captures/set_currents (инжект токов, квант ×16). Тест @t(0x1BE1C) §73.x: реконструкция по всем секторам + set_currents round-trip (устойчиво по seed 3/42/777). Тесты **155/155 PASS**.
| 2026-08-24 | **§73.8: модель замера скорости (SpeedModel) — замкнутый контур.** Разобран путь замера (вход PID 0x1d078): V=u32[RAM+0x158]=timer-capture период (энкодер), F=byte[RAM+0x100] гейт; формула (0x1d874, вериф. прямым вызовом) val=s16(48000/V) if F≠0 else 0 → u16[RAM+0x1768] (V=1000→48, V=24000→2, F=0→0). ISR-писатель V не найден статически (нет `str #0x158`) ни writer-sweep'ом по 503 функциям (0 хитов) — запись гейтится по аппаратному состоянию (импульс/pending), в изоляции не триггерится → модель параметризована. Класс SpeedModel: set_period/set_speed (V=48000//val)/stop/get_val. Тест @t(0x1D078) §73.x closed loop: set_period→PID val=s16(48000/V), set_speed (делители 48000)→ровно val, stop→val=0 (устойчиво по seed 2/55/2024). **Замкнутый контур «замер скорости → PID 0x1d078 → throttle» полностью управляем из app.** Тесты **156/156 PASS**.
| 2026-08-24 | **§73.9: модель динамики мотора (MotorModel) — автономный симул.** Последняя часть контура — plant (throttle→скорость во времени), НЕ firmware → параметризована: first-order lag `v_term=v_max·clamp(thr/thr_ref,0,1)`, `speed+=(dt/tau)(v_term−speed)`. **Ключевое открытие:** throttle S+0x64 принудительно=0 при u1760==0 (RESET anti-windup, §72) — u1760=«разрешение мощности», для работающего контура нужно u1760≠0 + гейты RAM+0x333/0x263=0 + counter S+0x28. Класс MotorModel: step/reset. Тест @t(0x1D078) §73.x motor dynamics — **АВТОНОМНЫЙ симул** target=208 (PID→throttle→plant→SpeedModel→PID, 40 итераций): скорость 0→overshoot~338→settle~207, throttle спадает; детерминированно (fixed params), устойчиво по seed 1/99. **Эмулятор моторного контура завершён: полный автономный симул замкнутого контура работает и сходится к target.** Тесты **157/157 PASS**.
| 2026-08-24 | **§73.10: эмуляция аккумулятора (BatteryModel) + уточнение plant.** Батарея: SoC%=u16[RAM+0x306] (из i16@0x17a0 [415..535]→[0..100]%, §25); FOC пороги 90/10, гейт byte@0x22e. **Проверено:** изолированный FOC идентичен для SoC∈{0,5,50,90,95,100} → пороги гейтятся, в изоляции не срабатывают. Класс BatteryModel: set_soc/get_soc/discharge (SoC падает с нагрузкой). Тест @t(0x1DFD8) §73.x battery: round-trip, mapping 0%→415/100%→535, разряд монотонен bounded [0,100] (устойчиво по seed 4/77/313). **Plant уточнён:** v_max по умолчанию=522 (макс. target §71≈25 км/ч); единицы ~20.8 units/(км/ч); tau/throttle_ref параметризованы (под live-замер). Тесты **158/158 PASS**.
| 2026-08-24 | **§73.11: эмуляция запаса хода (RangeModel, 0x1d898).** Разбор: `0x1d898` читает входной i16@RAM+0x27A (батарея, ADC-регион §25) через указатель, работает на общем struct=RAM+0x3C8. Выходы: **+0x20=X=(8000·v)>>16**; **+0x40=R=10000·X/(500−X)** (делитель 0x19994; div-by-zero→-1, отриц. знаменатель→отриц.). Формула вериф. **random-sweep 400/400 (v≥0)**. Класс RangeModel: set_value/estimate. Тест @t(0x1D898): sweep 150/150 + edge-cases v=0/415/535/4096 (устойчиво по seed 3/88/402). Физ. единица R не определена статически (live-корреляция); при SoC R≈1111..1494. Тесты **159/159 PASS**.
| 2026-08-24 | **§73.12: полная карта выходов 0x1d898 (аккумулятор + статус-флаги).** Write-capture (v=500, чистый emu): v-зависимых выходов только +0x20/+0x40 (§73.11). **Аккумулятор (leaky integrator):** new_0x3c=(old_0x3c+delta−prev_0x14)&0xFFFFFFFF, delta=i16@RAM+0x26E, new_0x14=asr(new_0x3c,10) — вериф. stateful-sweep 250/250. **Статус-флаги** (0x290=2, 0x292=140, 0x2a8=1, 0x2aa=78) — константны, state-machine (не v-зависимы); полный dispatch = отдельная задача. **Нюанс:** 0x1d898 state-зависим (поздние под-вызовы data-driven jump в unmapped при dirty-RAM) → тест acc использует fresh McuEmu/call. RangeModel: +set_delta/acc_step. Тест @t(0x1D898) range-acc: sweep 80/80 + edge-cases. Тесты **160/160 PASS**.
| 2026-08-24 | **§73.13: state-machine 0x1d898 — полная карта I/O + гейты статус-флагов.** Read/write-trace (чистый emu): читает 156 / пишет 27 RAM-off. Ключевые входы: v(+0x27a), SoC(+0x306), гейты +0x286/+0x29e, LUT-блоки 0x614-0x6c4/0x738-0x808 (калибровка). Write-source-mapping: +0x292←0x1d906, +0x290←0x1d920(>>6), +0x2aa←0x1d946, +0x2a8←0x1d95e (через под-вызовы 0x1de0d/0x1a639/0x1a011). **Гейты (эмпирика):** в zeroed статус-флаги константны, не зависят от v/SoC; +0x286≠0→(0x290,0x292)=(0,0), +0x29e≠0→(0x2a8,0x2aa)=(0,0); дефолт (2,140,1,78). **Вывод:** флаги mode/LUT-driven, физ. смысл = live-LUT + разбор под-вызовов (отдельная задача). Тест @t(0x1D898) range-sm: дефолт+гейты (fresh emu/call). Тесты **161/161 PASS**.
| 2026-08-24 | **§73.14: FOC в автономном контуре — throttle→current-ref связь + transfer-функция.** PID 0x1d078 пишет throttle=s16[RAM+0x42c]; FOC 0x1a938 не читает 0x42c, его current-ref = **u16[RAM+0x224]** (sensitivity-sweep: только он ломает симметрию CCR). Связь 0x224=throttle (1:1); коннектор-функция в FOC-кластере (пул 0x20000224 @ 0x1ad90/0x1baa4/0x1c74c/0x1d310). **Transfer-функция:** CCR-источники RAM+0x382/384/386 (PC 0x1b5d2/da/e2, duty_X=r3−v_X−base); amp≈0.0103·|ref| монотонно (0→0, 4000→41, 8000→83, 16384→169, 28624→296, 32767→338), center≈1125..1131, CCR_A↑/CCR_B↓ (2-фазная). **Интегрированный сим** `_foc_sim.py`: один McuEmu, общий RAM, каждый шаг PID+FOC; target=208→speed 0→~380→~318, target=300→0→~430→~394 (overshoot = rough plant, не live-калиброван). Тесты @t(0x1A938) FOC-transfer + @t(0x1D078) FOC-контур. Тесты **163/163 PASS**.
| 2026-08-24 | **§74.1: main-loop entry — статический потолок (RAM function-pointer dispatch).** Искали точный main-loop entry (подготовка к cold-boot, Фаза ③). **Вывод: статически не определяется** — main-loop + task-таблицы = runtime RAM state. Установлено: reset-handler (0x315c) init→возврат через trampoline (НЕ входит в main-loop); **SysTick = источник тика** (0x3600: LOAD=val/0x3E8−1, CTRL=7); **мажорные функции диспатчатся через RAM function-pointer, НЕ стат. `bl`** — PID 0x1d078 / FOC 0x1a938 / scheduler 0x1f600 / clock-init 0x3600/0x30e0 все с **0 статических вызывающих** (полный скан bl+литералов); механизмы: command-dispatcher 0x2e0c (r0=ID→handler) + USART3 TX frame-слоты @0xa43. Кандидаты «main-loop» по backward-циклам (0x02eb7/0x11a23/0x0285f) — I2C/clock init + retry-loop'ы, не top-level цикл. **Для cold-boot нужен live RAM-dump при reset** (класс C, SWD GD32): начальный стек @SP_init=0x20004e40 (trampoline-цели) + task function-pointer таблицы. Тесты 165/165 PASS (без изменений; §74.1 — статический анализ).
| 2026-08-24 | **§74: автономный прогон Фаза ① — `ControlLoop` (warm-start harness) + архитектура boot.** Разбор boot от reset 0x315c: init-цепочка (0xcd0d→0xcc69/0xc9dd/0x5971/0xcb41) исполняется чисто с periph=0xFF (~3–6k insn); **блокер cold-boot = несколько `pop {pc}` trampoline** (читают [SP_init+N], SP_init=0x20004e40, в эмуляции 0→прыжок в 0; C-runtime stack state). **Main-loop = планировщик 0x1f600** (round-robin 4 слота: slot_index=byte@0x2c2 wrap 0→3, диспатч при elapsed(now u64@0x1e0−last)>432 тика, дескриптор slot_idx×0x96(150B)@0xa43); task-указатели устанавливаются runtime (нет прямых `bl` к PID/FOC) → полный диспатч boot-blocked. **ControlLoop** (`emulator/mcu_emu.py`): warm-start — не boot/reset и не scheduler, PID 0x1d078 + FOC 0x1a938 вызываются напрямую на реальной частоте (валидная абстракция контура); tick: speed→SpeedModel(V)→PID→throttle 0x42c→0x224→FOC→PWM CCR→MotorModel(plant v_max=522/tau=15/tref=28624)→speed; + BatteryModel.discharge→SoC, RangeModel.estimate. Один McuEmu, общий RAM. **Валидация:** траектория совпадает с `_foc_sim.py` **40/40** (target=208); сходится ВЫШЕ target (~325) — свойство калибровки plant/PID (reference target=300→~394), НЕ баг. **Фикс BatteryModel.discharge:** set_soc тринковал self.soc до int/tick → разряд 1%/tick при любой нагрузке>0; теперь float-накопление, int в RAM (тест батареи не сломан). Тест @t(0x1D078) ControlLoop. Тесты **165/165 PASS**.
| 2026-08-24 | **§73.15: коннектор current-ref (уточнение §73.14) + оценка электро-контура.** Разбор FOC (пул 0x1ad90): PC 0x1aada `r3=[0x224]`, `cmp r3,#0x7fff` (пул 0x1ad50), `str r3,[0x388]` (пул 0x1ad94) → **P=u32[RAM+0x388]=[u16 RAM+0x224] (1:1)**, подтверждено эмпирически. P — вход cross+dot (§60.4) → PWM; **0x224 definitively = current-ref**. **0x224 — общий upstream-вход:** PID читает его (PC 0x1d12c, gated <<2), FOC только читает; пишут upstream (app/BLE), не PID/FOC (PWL 0x1c34c пишет -1/0 = очистка). **Уточнение:** «0x224=throttle(0x42c) 1:1» — approximation сима; реально 0x224=upstream-команда, 0x42c=выход PID. **Электро-контур не закрывается статически:** measured vector 0x108/0x10a и токи 0x838/83c/840 не влияют на PWM в изоляции (ни gate=0, ни STAT bit15=1) — нужен полный state-machine + live-gains мотора. Тест @t(0x1A938) current-ref→P + feedback-неактивен. Тесты **164/164 PASS**.
