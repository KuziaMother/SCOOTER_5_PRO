# TODO_RESEARCH — открытые задачи по декомпиляции/пониманию MCU и BLE

Группировка по классу доступа: **A** — статический RE (только файлы), **B** — живое
устройство (снуп USART3 / Mi Home, без доработки железа), **C** — аппаратный доступ
(SWD / UART-тест-точки). Статус «что уже сделано» — в [`PROGRESS.md`](PROGRESS.md);
детали и доказательства — в разделах [`REPORT.md`](REPORT.md) (указаны у каждой задачи).

Обновлено: **2026-08-24** (§59.7/§59.8: «own» round 0x1DD8C разобран и верифицирован (CRC-8 poly 0x1B Feistel); периодический таск USART3 0x1DFD8 — inline-логика верифицирована (3 утечных интегратора, % батареи [415,535]→[0,100]); эмуляторная верификация `scripts/mcu/func_verify.py` — **136/136 PASS**; карта MCU 96,8%; B-задачи: чип на I2C2, NVRAM-зоны, расширить верификацию)

---

## A. Статический RE (из файлов, без железа)

_(оригинальный список завершён — см. таблицу ниже; новые задачи из §48:_

- [x] **Гигант 0x17xxx (§48.5, §54) — ЗАКРЫТ (§55).** Дыра 0x174F2..0x19A1C =
      регион данных (версия-строки, float-константы, u16-таблицы, кривая 716→335,
      DFU-команды @0x1893C, модель "SZMC-ES-ZM-0283M" @0x19810) + 6 малых функций
      (boot-кластер 0x174FC/0x17524/0x17554 + делятели 0x19968/0x19994/0x199BC).
      «Несуществующая» гигантская функция — артефакт детектора на данных. — **§55**
- [ ] **Карта регистров периферии (нестандартная раскладка).** USART: SR@+0x3C/ICER@+0x40
      (не F1-class); блок @0x40003000 (драйвер 0x99xx, magic 0xAAAA/0xCCCC — адрес свободен
      в карте STM32F1/GD32F103); GPIOA+0x18/+0x28 (init 0x8xxx); ADC1+0x18 (биты 3/5);
      AFIO-зона @0x40010414; WWDG @0x40002800 (SR clear + магия 0xCA/0x53 в +0x24); DMA1 INTFR
      биты 0x02000000/0x20000. **§50-уточнения:** NVIC-база = 0xE000E400 (не 0xE000E100!);
      SPI1 = 0x40013000; таймер-кластер @0x40012C00..0x40014C00 (5 блоков, CCR-подобные +0x30/
      +0x2C); HSE = 20 МГц. Идентификация кристалла/раскладки по usage-паттернам. — **§48, §49, §50**)
- [ ] **NVRAM-зона @0x1A550 (flash).** `0x2d5c` пишет 26 байт в flash 0x1A550 (после записи
      magic 0xAAAA в @0x40003000, §49.1.14); отдельно `0x5c9c` пишет 2Б из @0x19B14. Что
      хранится в этой зоне (калибровки? счётчик циклов?) — сравнить с NVRAM-картой §37
      и/или дампом после езды. — **§49**)
- [ ] **NVRAM-зоны @0x304C/0x305C/0x30CF/0x30DD (RAM-копии с CRC).** §50: четыре зоны
      верифицируются CRC-16/CRC-8 после чтения (0xcf b8/0xcf60/0x147ac/0x13b14); @0x305C
      получает флаг OK/FAIL в u16@+0x24. Что хранят (калибровки мотора? параметры BMS?) —
      сравнить с NVRAM-картой §37 и дампом после езды. — **§50**)
- [ ] **Артефакты детектора: gap 0xC63E..0xC684 (§50.7.3).** Две пропущенные функции
      0xC644 (RCC+0x28) / 0xC664 (RCC+0x1C) по 26 Б + пулы — в каталоге с пометкой
      «НЕ канонич. функция». Решить: добавить в functions_mcu/README.md вручную
      (+52 Б к карте, ~+0,05 п.п.) или оставить как пометку (класс артефакта §48).
- [ ] **Расширить эмуляторную верификацию (§50.7–§59.4) на крупные функции.**
      `scripts/mcu/func_verify.py` — 129/129 PASS (~19% «разобран»);
      `--list` показывает покрытие. Все полки и дыры закрыты, чистого «ID»-кода
      нет; «частично» осталось **1** (0x1A938, 3296 B; 0x1DD8C и 0x1DFD8 закрыты в
      §59.7/§59.8). **0x1A938 (§59.9): это моторный FOC-рутин (TIM-capture/
      quadrant/2D-rotate), а не «батарейные пороги»** — полная верификация требует
      отдельного глубокого прохода. **Глубокий проход (§60/§60.1):** подфункции
      0x1be1c (TIM capture → FOC реконструкция фаз, 6-секторная), 0x1e410
      (табличная декодировка Q15-вектора @flash 0xA6C6) и 0x1d7ac (cross+dot двух
      Q15-векторов), 0x1d818 (cross+dot из RAM+0x10c), 0x1bd88 (6-секторный
      классификатор) разобраны и верифицированы — **ВСЕ 5 подфункций**; архитектура
      state-machine описана трассировкой (§60.3); inline-блоки 0x1b3f2 (inlined
      cross+dot, §60.4) и 0x1aa08 (кламп параметров + leaky-интегратор, §60.5)
      верифицированы.
      Дальше по выбору:
      **завершить глубокий проход 0x1A938** (остался inline-блок 0x1b48a..0x1b5f4 —
      vendor-инструкции, много pool-адресов; закроет последний «частично» → карта
      ~98.5%),
      response-сборщики RX-парсера ('a'/'B'..'J', §59.6), motor SM 0x7494 целиком,
      fixed-point 0x1c34c (17 pool-адресов RAM), BLE-сессии 0x6ccc/0x6e50
      (0x16f42 разобран в §57: OOB-баг на текущих таблицах — верифицировать только
      fault-путь). Ограничения: баг Unicorn со
      сдвигами по регистру (cnt≥256, (cnt&0xFF)≤28); «скрытые» логические
      immediate-сдвиги — тип только по исполнению; signed/unsigned ветки (bgt/bhi)
      различаются на бите opcode; **Python: `a + x >> n` = `(a+x) >> n`** — явные
      скобки в референсах; **capstone: odd-старт (off|1) сдвигает адреса и bl-цели
      на +1** (§53.2); **Run.call не сбрасывает R4–R12 — передавать ВСЕ аргументы**
      (§55.2); **Unicorn 2.1.4: числовые индексы регистров ≠ архитектурным (R0–R9
      = 66–75) — только константы arm_const** (§55.2/§56.5). — **§50.7/§56.4**)

## B. Живое устройство (снуп USART3 / I2C2 / Mi Home)

- [ ] **Идентификация чипа на I2C2 (dev 0x3E).** §49: 22 shadow-setter'а (RAM @0x15FA..0x164C)
      пишут в регистры 3/5/7, 0x12..0x2A, 0x32..0x3A, 0x70, 0x7F; чтение — 16-битный код
      регистра (0x1bdc/0x1c1c), результат → u16@0xF95+0x1E/0x20 (структура §22). Снуп шины
      I2C2 (SCO) + сравнение регистров с даташитами кандидатов (моторный контроллер / IMU /
      BMS-хуб). §50: протокол — CRC-7 stream-кодирование (0x15640), batch write 0x11B
      (0x280c), charger-регистр 0x9335 (0x3668). **§52: чип имеет memory-mapped EEPROM-
      регионы**: окно 0x21000..0x25E20 (40B-страницы, page-reader 0x157e0) и
      0x41000..0x45000 (32B-страницы, 0x14802) + 2KB-блок @+0x800 (0x119e4) — кандидат
      с NVRAM/EEPROM (BMS-хуб или моторный контроллер с памятью). — **§49, §50, §52**

- [x] **Аномалия hdr таблиц интерполяции @0x19F60/0x19F58 (§53) — ЗАКРЫТА (§56).**
      Коррекция фрейма (bl не пушит lr): r6=ygrid, sb=hdr-указатель. hdr-слова
      u32[база+0/+4] читаются ТОЛЬКО при v1>xtab[0]=2002; caller 0x69e4 передаёт
      v1=s8∈[−128..127] → путь недостижим, всегда early-exit с константой
      grid[0]=0x3A08 (эмуляторно подтверждено, тест в батче 9). xtab {2002..63734}
      — кривая по полному u16-диапазону под другой вызывающий; латентный баг
      версии. Динамической подмены таблицы не требуется для объяснения. — **§56**


- [ ] **MCU: физический привод моторных FET (остаток A1, §41).** Статика+эмуляция
      доказали: в mcu_0007.bin duty% (byte@RAM[0xFD3]) НЕ применяется к TIM1 CCR/GPIO/DMA
      на runtime; регулятор вырожден (выход всегда ≈ -275 → duty 0%). Кто реально модулирует
      привод — вне этого образа. Проверка: (а) live-снуп TIM1 CCR1/2/3 (0x40012C44/48/4C)
      и GPIO-блока при езде (SWD); (б) дамп BLE-app (RTL8762C) — свой ШИМ?; (в) осмотр
      платы (отдельный драйвер?). Таблица @0x17d3a (8×8 нибблов после `55 AA`) — без
      кодовых ссылок, проверить при живом снупе. — §41
- [ ] **MCU: физический смысл 3-проводной шины режима** (пины 6-8 порта1, `byte@0x26b`;
      джамперы/BMS/свитч?) + расширенные биты кастомного DMA (0x5D000041, контроллер
      @0x40022000). Живой снуп / даташит. — §40.7
- [ ] **siid/piid ↔ CMD 0x41..0x4B (MCU-сторона).** App-сторона закрыта (§45.1: полный
      свип 88/88, живые значения; SET-механика верифицирована §45.3; `probes/prop_sweep.py`).
      Осталось: параллельный перехват USART3 при тех же read/write → маппинг
      «siid/piid ↔ sub-команда MCU». — §35.3, **§45**
- [ ] **Живой пуш-захват.** Механизм и парсер готовы. **§45.4: app-инициированные SET
      (свет/atmo/TCS/режим/замок) пушей НЕ дают** — устройство пушит только автономные
      события. **§47: MCU-сторона закрыта** — пуши = кадры 'a0'/'a1' из сборщика `0x211f8`
      (TX-кольцо @0x10b5, период ≈ 1,9–3,2 с). Нужна **езда**: газ/движение → ожидание
      пушей 1.5 CURRENT / 1.6 POWER / 2.1 AVERAGE_SPEED / 2.7 IS_RIDING + сверка значений
      с байтами рамки 'a0' (bias-кодирование i16@0x31c/0x290, u16@0x236 BE). — §18, **§45**, **§47**
- [x] **FOTA-захват из Mi Home (обновление BLE).** **ЗАКРЫТО статикой (§42):** в репо уже
      были реальные пакеты (`firmware_ota/`); формат = контент + PEM (Mijia Open→cert) +
      ECDSA-SHA256 от SHA-256(контента до PEM); ОБА пакета верифицированы офлайн
      (`scripts/mcu/verify_fota.py`). Живой захват теперь нужен только для наблюдения за
      протоколом передачи (порядок/ретрансмиссии), формат ясен. — §30.6, §38, **§42**
- [ ] **u16@0x2000032e/0x330 (get 0x36).** Читатель найден: sub 0x36 → ответ `64 36` (§36.5);
      CPU-писателя в образе нет (§26.4) — USART3-снуп (+ динамика во времени) или SWD-RAM.
      *Открывает:* единицы и источник значения. — §31.6.1, §36.5
- [ ] **Живая верификация «own»-рукопожатия.** Статика (§37): инициатор — BLE-чип
      (кадры `64 6F 77 6E 20 <cmd>` = "down …" по USART3, стейт-машина M[0x180]:
      0→0xa→0xb→0xc→0xd→0xe). Осталось: **когда** (power-on / bind / периодичность);
      снуп USART3 в момент power-on/bind. *Модель `crypto_own.py` позволяет офлайн
      вычислить любой ответ MCU* — нужен только сид @0x871..@0x880. **§45.5: сид НЕ
      тригурируется стандартными siid/piid-чтениями** (свип 88/88) → получение сида:
      дамп NVRAM flash @0xF8B8 (класс C, rtltool/SWD) или rebind (деструктивно).
      + сверка снупленных кадров 'b'/'m' с моделью.
      *Открывает:* роль шифроблока `0x21c64` и маски @0x1565 в связке с BLE-чипом. — §36.2, §37, **§45**
- [ ] **Направление переключения режима TX-кольца (432↔80 тиков) при byte@0x358=1.**
      **§47.1: роль @0x358 уточнена** — при =1 телеметрия 'a'-кадров подавлена (гейт
      сборщика 0x211f8, вместе с @0x339). Само направление переключения (до/после
      «own»-обмена) — по-прежнему снуп UART4 + USART3. — §28.5, §36.4, **§47**
- [ ] **Blob-конфиги @0x84b/@0x85d/@0x871.** Захват кадров 0x15/0x16/0x1a из Mi Home при
      реальной привязке + дамп @0x16aa после расшифровки (или разбор APK-генератора). — §23.4, §27.4
- [ ] **Единицы push-значений.** i16@0x31c / i16@0x290 (обработанные ADC-каналы) и
      u16@0x236 (=val×100/208, %). **§47.2: транспорт ясен** — i16 уходят в кадр 'a0'
      с bias-кодированием (+7/+8), u16@0x236 — BE (+0xa/+0xb); BLE-чип ре-кодирует в GATT.
      Осталась корреляция с датчиками по живому (езда). — §34, **§47**
- [ ] **Режимы `byte@0x229` (0x02/0x03/0x0B) — MCU-сторона.** **§45.3: live верифицировано**
      переключение RIDING_MODE (1.1) по BLE: SET 2→read 2, SET 11→read 0x0B, restore 3.
      Осталось: наблюдение byte@0x229 в RAM и кадров MCU при смене (USART3-снуп/SWD).
      *Открывает:* state-машина режимов ~`0x1d0c6`. — §34.2, **§45**
- [ ] **Абсолютные единицы `i16@0x17a0`.** Калибровка [415..535]→% известна, «сырьё» — нет;
      живой снуп. — §31.6.3
- [ ] **RAM-записи RX-хендлеров 0x40/0x63 (группа A).** Эмуляция заблокирована data-driven
      вызовом (§26) — живой снуп. — §31.6.9
- [ ] **Мастер V и i16@0x272 [1876..2275] (канал 0x5a).** Физические единицы + кто генерирует
      0x5a-кадры (BLE-чип по своим датчикам или ретрансляция команд приложения). — §24.4
- [ ] **ADC buf[0..4] + канал-триггер.** Физические сигналы (пины → датчики): схема/
      осциллограф или снуп. *Открывает:* полный контур сенсоров. — §25.7, §31.6.2
- [ ] **Блок `0x40023c00`.** Природа (BMS-мост / специализированный фронтенд?) и единицы
      RESULT — статически не определяется. — §25.7
- [ ] **Регион-проверка по SN на 5 Pro.** Живая проверка: смена региона в приложении +
      наблюдение за скоростью/поведением (на 4 Pro энфорсмент в контроллере, §17). — §15

## C. Аппаратный доступ (SWD / UART-тест-точки)

- [ ] **SWD-проба RTL8762C (BLE).** Сначала физически проверить, отвечает ли SWD вообще
      (Level 0? / за паролем?) — до любых чтений. Если открыт → дамп XIP-региона
      `0x0180E000` (100 КБ) = **расшифрованное BLE-app** (чтение идёт через шину с
      inline-декриптом, ключ знать не нужно). Метод: ST-Link/J-Link + OpenOCD
      (`debug/rtl8762c.cfg`, TAPID `0x2ba01477`). *Закрывает:* главный блокер BLE-части. — §6.11, §19.2
- [ ] **SWD-проба GD32 (MCU).** ⚠️ На 4 Pro RDP включён: сначала статус RDP **без unlock**
      (unlock уничтожает flash — дамп может быть деструктивным). Если открыт → полный образ
      с вектор-таблицей (снимает блокер широкой эмуляции §16.3) + рантайм-RAM: таблица
      команд @0x20000A43, счётчики, NVM (шаблон 4 Pro: SN@0xa8, UUID-привязка@0x1b4,
      пробег@0x52). — §17
- [ ] **UART-дамп RTL8762C через rtltool** (тест-точки GND/RX/TX/RST/LOG, LOG→GND при сбросе).
      Приоритет низкий: при XIP-шифровании дамп = шифртекст (§6.11); полезен для сверки
      flash-раскладки/OEM Header и как дешёвая проба «OTA AES Key» (транспортный ключ §19.1). — §6.10, §19.1–19.2
- [ ] **Писатель @0x33b (16 Б, источник XOR-маски «own»).** В данных-канале только читается;
      статических писателей нет → SWD-RAM (дамп @0x2000033b до/после «ok»-обмена) + сверка
      с NVRAM-картой. — §36.2, §36.7

---

## Закрытые (для справки)

| Задача | Когда закрыта | Где |
|---|---|---|
| **Mijia Root CA (корень FOTA-цепочки)** | 2026-08-24, статика: корень вшит в APK Mi Home — `assets/MijiaRootCert.der` (self-signed `O=Mijia Root, C=CN`, P-256, 2016..2066); SKI = AKI c0 ✓; полная цепочка Root→c0→c1 верифицирована (openssl + ручная проверка ключом root); `verify_fota.py` теперь до trust anchor, оба пакета OK | §44 |
| **A2: Realtek SDK default OTA AES-ключ** | 2026-08-24, статика (полное SDK 424 МБ + MP Tool v1.1.2.4 + FW Realtek): **такого ключа нет**. T_ENC_KEY_SELECT: SCEK/OCEK (eFuse) [+RTK/OEMCONST в boot ROM] / ON_FLASH (dec_key[16] в хедере — у Dreame нули). OCEK per-chip с производства (XML data_protection/key_gen). Явные кандидаты (uuid/zero/xiaomi/miot × CBC/CTR) — 0. Корректировка §6.11: файл = RomPatch@0x0 (xip=0, enc=1, select=1=SCEK_WITH_RTKCONST, exe_base=RAM 0x203800) + AppPatch@0xA000 (xip=1, enc=0, select=0=SCEK; payload 100% шифр); «select=7» — ошибка битов. SWD-план уточнён: RAM-дамп @0x203800 = RomPatch-load без ключа | §43 |
| **A3: структура ECDSA-подписи трейлера + офлайн-верификация** | 2026-08-24, статика: подпись стандартная DER ECDSA P-256 (RFC 5480; «поле 03 ff» — байты внутри r, ошибка парсинга §38.5); подписано SHA-256(всё до первого BEGIN PEM); ключ — leaf «cert» (цепочка c1←c0 валидна); **оба реальных FOTA-пакета из Mi Home верифицированы** (MCU d[:0x24845], BLE d[:0x2542D]); FOTA-пакет MCU = mcu_0007.bin побайтово; верификатор `verify_fota.py`; B1 закрыт следом. Остаток: публичный Mijia Root (новая A-задача) | §42 |
| **A1: применение duty к мотору (остаток §39)** | 2026-08-24, статика+эмуляция (Unicorn, live-seed): полная цепочка `0x799c` (регулятор) → slot-3 state-machine (TBB @0x7AA4) → slew `0xE408` → u16@RAM[0x1357] → duty% = byte@RAM[0xFD3]; потребители duty% — только телеметрия/мониторы; **runtime-записей в CCR нет** (скан str по 5 таймерам + 30 итераций эмуляции × 3 ветки = 0 периф-записей); регулятор вырожден: выход всегда ∈ [-290,-258] при любом i16-сенсоре и mode → duty 0%; единственные CCR-записи: boot `0x1A894` (CCR=2250) и калибровка `0x1CD0A` (CCR=2/2249). Корректировки: TBB PC=(addr+4)&~3, `0x1A894` = boot-CCR-init, «BDTR=2249» = CCR4, mode-getter читает byte@RAM[0xFC8]. Физический привод — новая B-задача | §41 |
| «Альтернативные ADC-режимы» `0x1bf24`/`0x1a894`/`0x1e298` | 2026-08-23, статика+cross-ref: (а) 3-бит железный режим с пинов 6-8 порта1 (входы, cfg из `0x1bedc`) + дебаунс-машина `0x1a688` + стейт-машина `0x23374` (pass-through raw) → `byte@0x268`; (б) одноразовая boot-калибровка ADC (флаг `byte@0x246` ставит init `0x23480`, 2250 в common+0x4/8/C, busy-wait); (в) периодический re-arm DMA1 ch1 каждые ~65534 тиков (`byte@0x3C0`) + старт ADC (биты +0x18: 2/3/5) | §40 |
| ID команд опроса USART3 (рантайм-RAM @0x20000A43) | 2026-08-17, эмуляция дала реальные кадры | §16.1 |
| CRC16-алгоритм кадра / `55 AA`-заголовок | опровергнуты как ложные срабатывания; CHK = 0x63+CMD (TX) и вычитание/сложение (RX) | §6.6, §32, §35.1 |
| Формат RX-кадров и карта «поле → RAM» (все 15 CMD) | 2026-08-17, исполнением | §16.4 |
| Подкоманды данных-канала (32 шт.) и заголовки 0x51..0x54 | 2026-08-18 | §23, §29 |
| Писатель get 0x36 в образе | исчерпывающий поиск: CPU-писателя нет (NVRAM-restore/DMA) | §26.4 |
| Физический смысл C42/C43/C45 (протокольный уровень) | 2026-08-23, статика: это GET-пейлоады sub 0x23/0x24/0x26 (+C46 = sub 0x27); MCU не интерпретирует — физ. единицы ушли в B-класс | §36.4 |
| C48/C49/C4A: ретрансляция или неиспользуемость | 2026-08-23, статика: потребителей в MCU нет; MCU опрашивает BLE-чип on-demand (`63 48/49/4A` из sub 0x2C/0x2E/0x2F) — для MCU мёртвое хранилище | §36.4 |
| Писатель `byte@0x358` (режим TX-кольца) | 2026-08-23, статика: хвост «ok»-обмена протокола «own» (`0x20046`); направление переключения режимов — в B-класс | §36.4 |
| Читатель get 0x36 (u16@0x32e/@0x330) | 2026-08-23, статика: sub 0x36 → ответ `64 36` (BE); писатель остаётся NVRAM/DMA | §36.5 |
| Протокол управления/рукопожатия BLE-чип↔MCU | 2026-08-23, статика: кадры «own» (g/r/b/m/n/w), маска @0x1565, стейт @0x319, шифроблок `0x21c64→0x1a7ac` (S-box @0xa907) | §36.2–36.3 |
| N3: шифроблоки «own» vs конфиг #3 | 2026-08-23, статика+исполнение: разные функции; «S-box'ы» @0xa907/@0xab07 = срезы кода (не биективны); `own_cipher` — однонаправленная (коллизии 11k/65k); expand — 176 Б (не 48); ключ per-device из Mi Home (бинд→NVRAM→старт 0x1ca08); пересборка 'b'/'m' с корректировками §36.2 | §37 |
| Остаток N3: формат blob-конфигов #1/#2/#3 | 2026-08-23, статика+исполнение: запись @0x844..@0x880 = заголовок 7Б + ID платы `TK00323H0000001S73` (18Б) + дата-строка (20Б, флаг M[0x337]) + сид 16Б; дефолты из flash @0x1CC08/@0x1CC1C; NVRAM-карта скорректирована; GET 0x22/0x28/0x29/0x2a сняты (0x2a = сид raw); RX-гейт sub 0x1a: M[0x241]==1 блокирует | §38 |
| Строка из 35 цифр @0x24164 + `TK00323H` + «таблица 256×u32» @0x24000 | 2026-08-23: всё это — **build-трейлер образа** @0x236B8..@0x24D39 (ноль кодо-ссылок): u16-LUT (две вогнутые рампы, не раунд-функция) + 35 цифр (7×5-значных групп — SN партии/время build-лога, не регион) + манифест («…8-2023», `ETJSSZDDKJYXGSYFB2`) + **X.509 Mijia Root→Mijia Open→cert + ECDSA-подпись** (FOTA trust anchor; ключи извлечены в tmp_certs/) | §38, §30.7 |
| FOTA-подпись: чей это код | хвост — данные для внешнего верификатора, не исполняемый код MCU | §30.7 |
| Ключ шифрования BLE из файла | невозможно: XIP + per-chip eFuse (Level 2) | §6.2, §19.2 |
