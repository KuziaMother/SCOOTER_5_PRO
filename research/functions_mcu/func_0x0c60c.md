# func_0x0c60c

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x08000c60c) | `0x0000c60c` |
| размер кода | 18 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40021000 — периферия (r2)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  0c60c:  mov r1, r0                        
  0c60e:  movs r0, #0                       
  0c610:  ldr r2, [pc, #0xc]                -> периферия
  0c612:  ldr r0, [r2, #4]                  
  0c614:  bic r0, r0, #3                    
  0c618:  orrs r0, r1                       
  0c61a:  str r0, [r2, #4]                  
  0c61c:  bx lr                             
  ; --- literal-пул @0x0c620 (1 слов) — ВНЕ границ функции ---
  0c620:  .word 0x40021000  ; периферия
```
