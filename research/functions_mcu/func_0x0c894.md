# func_0x0c894

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x08000c894) | `0x0000c894` |
| размер кода | 10 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40021000 — периферия (r0)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  0c894:  ldr r0, [pc, #8]                  -> периферия
  0c896:  ldr r0, [r0, #4]                  
  0c898:  and r0, r0, #0xc                  
  0c89c:  bx lr                             
  ; --- literal-пул @0x0c8a0 (1 слов) — ВНЕ границ функции ---
  0c8a0:  .word 0x40021000  ; периферия
```
