# func_0x096dc

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x0800096dc) | `0x000096dc` |
| размер кода | 52 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x20000dd8 — RAM (r0)

## Вызовы (callees)

- 0x011a4 (bl, вне списка функций)
- 0x0970c (b, вне списка функций)
- 0x0ae98 (bl, вне списка функций)

## Кто вызывает (callers / xrefs)

- (не найден в коде образа)


## Дизассембляция

```asm
  096dc:  push {lr}                         
  096de:  sub sp, #0x24                     
  096e0:  ldr r0, [pc, #0x2c]               -> RAM
  096e2:  ldr.w r0, [r0, #0x114]            
  096e6:  cbz r0, #0x96f2                   
  096e8:  ldr r0, [pc, #0x24]               -> RAM
  096ea:  ldr.w r1, [r0, #0x114]            
  096ee:  blx r1                            
  096f0:  b #0x970c                         -> 0x0970c (вне списка функций)
  096f2:  movs r2, #0x24                    
  096f4:  ldr r1, [pc, #0x18]               -> RAM
  096f6:  adds r1, #0x10                    
  096f8:  mov r0, sp                        
  096fa:  bl #0x11a4                        -> 0x011a4 (вне списка функций)
  096fe:  ldr r0, [pc, #0x10]               -> RAM
  09700:  ldr r3, [r0, #0xc]                
  09702:  ldr r2, [r0, #8]                  
  09704:  ldr r1, [r0, #4]                  
  09706:  ldr r0, [r0]                      
  09708:  bl #0xae98                        -> 0x0ae98 (вне списка функций)
  0970c:  add sp, #0x24                     
  0970e:  pop {pc}                          
  ; --- literal-пул @0x09710 (1 слов) — ВНЕ границ функции ---
  09710:  .word 0x20000dd8  ; RAM
```
