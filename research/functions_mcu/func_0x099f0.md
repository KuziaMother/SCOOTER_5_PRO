# func_0x099f0

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x0800099f0) | `0x000099f0` |
| размер кода | 10 Б |
| регион | код B (FLASH-OTA: 0x06230/0x06304) |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- 0x40003000 — периферия (r1)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- `func_0x09a18` (bl @0x00009a1a)
- `func_0x09a20` (bl @0x00009a38)


## Дизассембляция

```asm
  099f0:  movw r0, #0xaaaa                  
  099f4:  ldr r1, [pc, #4]                  -> периферия
  099f6:  str r0, [r1]                      
  099f8:  bx lr                             
  ; --- literal-пул @0x099fc (1 слов) — ВНЕ границ функции ---
  099fc:  .word 0x40003000  ; периферия
```
