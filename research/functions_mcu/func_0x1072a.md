# func_0x1072a

| | |
|---|---|
| offset = vaddr (база 0x0; mirror 0x08001072a) | `0x0001072a` |
| размер кода | 6 Б |
| регион | код C |

## Строки (ссылки через literal-пулы / movw+movt)

- (нет)

## Литералы и адреса

- (нет)

## Вызовы (callees)

- (нет)

## Кто вызывает (callers / xrefs)

- `func_0x10870` (bl @0x000108ca)


## Дизассембляция

```asm
  1072a:  mov r1, r0                        
  1072c:  ldrh r0, [r1, #0xc]               
  1072e:  bx lr                             
```
